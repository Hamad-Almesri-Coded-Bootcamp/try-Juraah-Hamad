# P2-WP5 — Write paths and enforcement · task brief

Read `docs/briefs/P2-common.md` first. One subagent (Opus). Depends on Gate 3 (all reads) and Gate 4 (the engine). **The most dangerous package: the owner reviews Gate 5 line by line.** You implement the 17 remaining seam writes and prove every `ENFORCEMENT.md` row not already proved by WP2/WP3/WP6/WP7.

## FUNCTIONS YOU IMPLEMENT (`lib/data/pg/writes.ts`, `lib/data/refusals/writes.ts`, `lib/data/shapes/writes.ts` — your three files; WP1's §3 deviations say one-shot refusals often surface as 0 rows under RLS, not a raise — treat 0 rows as the refusal)

Patient and settings — `updatePatientPhone` · `completeOnboarding` · `updateSettings` (seam whitelist of the seven keys **and** the `settings_tracking_requires_link` trigger; `tracking_enabled`/`tracking_disabled` only when the flag flips; refusal for a non-owning session = the current row, as the mock).
Supply — `requestRefill` (insert; the `refill_routing` trigger derives `routed_to` and raises on ownership/`status` — map the raise to the mock's `{id:'', …}` refusal shape; divergence D-6).
Caregivers, patient side — `lookupMaskedName` (**session required**; `lookup_audit` row per call; rate limit 10 per 60 s per session → the `{maskedName:null}` shape; the account query runs on both branches; response built the same way on both; CR-043's trade-off is already logged) · `inviteCaregiver` (`pending`, `invited_at = jurah_now()`, `expires_at = +14 d`, `newId('cg')`, `caregiver_invited` with the masked name in the message when an account exists, else the mock's neutral wording — check §5) · `cancelInvitation` · `revokeCaregiver` (also revoke that caregiver's `sessions` rows).
Consent — `acceptInvitation` (the `caregiver_transitions` trigger is the enforcement; on success a new `sessions` row + signed cookie via WP2's `lib/session/cookie.ts`; returns the new `Session`; on refusal returns the caller's session as the mock) · `declineInvitation` (a pending-only session is revoked afterwards and the cookie cleared).
Caregiver shell — `selfUnlink`.
Clinic — `submitReviewDecision` (one-shot via `alert_review_once`; `reviewed_by = S.subjectId`) · `confirmPrescriptionFields` (**only** `drug.brandName`, `drug.strengthMg`, `frequencyPerDay`, `startDate`, `doseTimes` from `values`; `needs_review=false`, `confirmed`, reviewer fields; **call `regenerateUpcoming` from `lib/engine` FIRST, then apply the confirming update** (D-033 — the reviewer loses RLS access to the patient the moment the last queue item is confirmed); map the helpers' RLS raise to the refusal shape; refused unless `pending`) · `returnPrescriptionToClinic` (one-shot; **keep `needs_review = true`** on return — WP3b's queue query hides a returned row whose `needs_review` is false).

Also: use `lib/engine` (`insertGeneratedDoses`, `regenerateUpcoming`) for every dose generation — never a second generator call site. Also: `scripts/print-shapes.ts --backend=postgres` must now run every function without a throw; the mutation order in the script is what §5 recorded — keep it. On the Postgres run only: call `getInvitationForConsent(cg-08)` as سارة (D-024) and `readLastKnownSnapshot('getPatient:pt-01')` as حمد (the caller-scoped store, D-5); apply CR-041's opaque rule to `id` **and** `relatedId` on created/live rows when diffing.

## TESTS — `tests/integration/enforcement/`

`settings.test.ts` — **E-09, E-10, E-11, E-12**. `identity.test.ts` — **E-13 (full), E-14, E-15**. `invitation.test.ts` — **E-16 → E-24** re-run against your writes where a write is the proving call (E-17, E-19, E-20 job half is WP6's — coordinate: assert the read-time half here). `role.test.ts` — **E-29, E-30, E-33, E-34, E-35 (write half), E-42**. `client.test.ts` — **E-39** (a direct `sql` insert outside `append()` — the test may import `lib/db/client` because it is a test; guard 8 excludes `tests/`). `tests/integration/roundtrip/writes.test.ts` — every write's returned shape string-equal to `shapes.json` under CR-041's id rule. `tests/unit/data/refusals.test.ts` — every function's refusal literal in `refusals.ts` equals what `mock-impl.ts` returns for a `null` session (imports both; the mock is the arbiter). `docs/backend-notes/p2-wp5.md`.

## SPECIFICS THE OWNER WILL LOOK FOR

- **E-13 byte-for-byte:** capture the two `lookupMaskedName` responses as JSON strings and assert they differ only in the `maskedName` value; 200 timed samples each, paste medians and p95, distributions must overlap.
- **E-17:** attempt `acceptInvitation('cg-03')` as حمد, as م. دانة, as عبدالله, and as the owner role with a system GUC through a direct `update` — every one raises `caregiver_transitions` or is refused; the row stays `pending`; **grep the migrations for any `SECURITY DEFINER` function that writes `caregivers.status` and paste the (empty) result.**
- **E-33/E-34:** `confirmPrescriptionFields('rx-006', { status:'discontinued', patientId:'pt-01', source:{…}, drug:{ genericName:'X', strengthMg: 5 } } as any)` changes `strengthMg` only (and the four others when given) — `status`, `patientId`, `source`, `genericName` untouched; second call raises.
- **G1 in every write:** no function here touches `doses.status`; `confirmPrescriptionFields`' regeneration deletes and inserts `upcoming` rows only — assert سارة's recorded doses survive an unrelated regeneration and that the `doses` DELETE policy refuses a recorded row.
- **Audit messages** exactly as the mock's (§5): `أُضيفت وصفة …`, `طلب تعبئة …`, `دعوة مقدّم رعاية أُرسلت`, `مراجعة تنبيه — تأكيد/إخلاء`, `تأكيد بيانات وصفة …`, `أُعيدت وصفة … للعيادة`, `سُحبت صلاحية …`, `أُلغيت دعوة …`, `… قبل الدعوة`, `… رفضت الدعوة`, `… ألغى ربط نفسه`, `تفعيل/إيقاف متابعة الجرعات`. Every message scanned for `\d{12}`.

## ACCEPTANCE — Gate 5

Paste: every enforcement file's run with each row id visible · the round-trip writes run · `print-shapes --backend=postgres` full output (no throws) · the SECURITY DEFINER grep · `npm run verify` exit 0 in mock · `npm run guards` · frozen-set diff empty · every divergence appended. Without `JURAH_DATABASE_URL`: the loud NOT-A-PASS output plus MCP hand proofs for E-09, E-11, E-14, E-17, E-33, E-34, E-42 at minimum, SQL and result pasted.
