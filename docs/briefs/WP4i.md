# WP4 bundle i — Clinic (X0, G1s, G2s, G3s, X1) · task brief

Issued by the Phase 1 lead for wave 2 (after wave 1 passed its gates). One subagent. This bundle carries the demo's proof moment: the audit log filtered to dose-status writes shows no interface actor, ever. Format: `docs/Master Prompt — Phase 1.md` → TASK BRIEF TEMPLATE.

## OBJECTIVE
Build X0 (clinic entry + role chooser), G1s/G3s (the reviewer's two queues, switched in place), G2s (the reviewer decision with the read-only patient context), and X1 (the append-only audit log with its filters and the dose-status proof moment).

## READ — in this order, all of it
1. `CLAUDE.md` — rules 1, 6, 7 apply; the clinic route is role-gated filing, not security. 2. `docs/Acceptance Criteria and Test Plan.md` — inventory rows X0, G1s, G2s, G3s, X1 with pass criteria; the role model; G7, G9. 3. `docs/SCREENS.md` rows for the five screens (binding). 4. `docs/Seed Dataset.md` — `ia-001` pending (the queue's content), the flagged prescriptions `rx-006`/`rx-007` for G3s, سارة's five `dose_status_recorded` events (the proof moment: filtered, exactly those five, all agent/system actors), the full audit table, د. خالد (reviewer + admin), م. دانة (admin only). **Never invent a value.** 5. `docs/DECISIONS.md` — **CR-010 in full** (the X1 exception: the literal `actor.role`/`AuditEvent.type` string may appear on X1 only, beside its human label, never instead of it; masked name for the patient reference; no export control), CR-005 (د. خالد's dual role), CR-014 (`AuditLog` board deviations; the filtered list shows exactly سارة's five rows), CR-018, CR-028, CR-029 (chrome on every clinic screen — the two reviewer boards that omit it are wrong), CR-030 (a single-role clinic account gets no TabBar — `ClinicNav` already handles it), D-006, D-007 (the phone-width sign-out is provisional in the chrome; when your screens add their own AppBars, note — do not move — the chrome control; the lead reconciles), D-008. 6. `docs/UX Principles.md` — checklist, §12 (simulated-role banner), §6. 7. Boards: `ClinicEntry`, `ReviewerQueue`, `FieldQueue`, `ReviewerDecision`, `ReviewerDesktop` (1280), `AuditLog`, `AuditLog1440` (+ wireframes `README.md` first; remember CR-010/CR-029 where boards contradict the spec). 8. `components/ui/README/{TextField,Button,Countdown,Card,InlineNotice,AlertRow,ChoiceGroup,DetailRow,PrescriptionCard,SectorChip,DoseTimeline,Sheet,Select,ActivityRow,EmptyState,AppBar,ContextBanner}.md` — `ActivityRow`'s `layout: 'table'` variant is X1's 1440 rendering. 9. Existing code: `lib/session` (`signIn`, `getRoleOptions`, `chooseRole`), `lib/data` (`getReviewQueue`, `getFieldConfirmationQueue`, `getAlertForReview`, `submitReviewDecision`, `getFlaggedPrescription`, `confirmPrescriptionFields`, `returnPrescriptionToClinic`, `getAuditLog`), `features/identity/` (bundle b's sign-in — **reuse its exported sign-in form/countdown composition if it exports one; X0 is "same simulation, same states as A1"**; if nothing reusable is exported, report it and compose the same states from the ui components with your own catalogue copy), `app/[locale]/clinic/layout.tsx` + `features/shell/ClinicNav.tsx` (WP3's chrome — import nothing, it wraps you; do not edit), `tests/e2e/helpers/session.ts`.

## FILES YOU OWN
`app/[locale]/clinic/page.tsx` · `app/[locale]/clinic/choose/page.tsx` · `app/[locale]/clinic/review/page.tsx` · `app/[locale]/clinic/review/[alertId]/page.tsx` · `app/[locale]/clinic/review/fields/page.tsx` · `app/[locale]/clinic/review/fields/[prescriptionId]/page.tsx` · `app/[locale]/clinic/audit/page.tsx` (all currently placeholders) · `features/clinic/**` · `i18n/copy/clinic.ts` (stub exists; fill it) · `docs/backend-notes/wp4i.md` · `tests/e2e/clinic.spec.ts` · `tests/unit/clinic/**`.

## FILES YOU MUST NOT TOUCH
`components/ui/**` · every layout and `features/shell/**` · every other `features/*` (import only) · `lib/**`, `types/**` · `i18n/**` except `copy/clinic.ts` (dose-status/review-state words from `vocabulary.ts` — reuse, never redefine) · `scripts/**` · existing tests · other bundles' routes · `docs/**` except your fragment.

## DEPENDENCIES
WP3's clinic layout gates every route, renders the simulated-role banner on all of them, and builds the TabBar/rail from the session's roles (one destination → no bar, CR-030). D-006: a wrong-role session redirects to `/gate` — your tests assert it, the chrome does it.

## WHAT TO BUILD (per SCREENS.md, which wins over this summary)
- **X0** (`/clinic`, `/clinic/choose`): Civil ID sign-in, same simulation and states as A1 (countdown, lapsed, retry, input kept); **a non-clinic ID is refused with the same neutral wording as A1's `no_claims` — never revealing whether the ID has an account or what roles it holds** (rule 6); labelled simulated; states in words that access is enforced server-side by role, not by the address being unlisted; the chooser (two equal Cards, medical review / system administration) only for an ID holding both (د. خالد); never linked from L1 or the patient app (nothing for you to do — just never add such a link).
- **G1s** (`/clinic/review`): every `pending_medical_review` alert, most severe first; per row: patient **first name**, the drugs, waiting time derived from `createdAt` vs `REFERENCE_NOW` **by the data layer's view, not screen arithmetic** (if `getReviewQueue` does not supply it, report — do not compute from a clock); empty queue state; a segmented `ChoiceGroup` switching in place to G3s.
- **G2s** (`/clinic/review/[alertId]`): the alert description; `sourceCitation` verbatim or the explicit "to be supplied" line (never invented); involved prescriptions with clinical fields; the **read-only patient context panel** from `getAlertForReview` (full active list with facility + sector, recent dose history as `DoseTimeline` — with statuses only when the context says tracking is on); **confirm the risk / clear it**, optional note (`TextField`, D-004), each committing only through a `Sheet` confirmation; after commit → back to G1s with the item gone; **no Prescription field and no Dose is editable**; the reviewer reaches no patient except through a queue item (no free patient lookup exists — assert your module graph calls only the five clinic functions plus session).
- **G3s** (`/clinic/review/fields`, detail `/fields/[prescriptionId]`): flagged prescriptions with uncertain fields marked and the source image (with alt); **confirm the field values** (`confirmPrescriptionFields` — values pre-filled from the record for confirmation; editing a value before confirming is the reviewer's job, so `TextField`s are permitted HERE and only here) or **return to the issuing clinic** with a reason (`returnPrescriptionToClinic`), each through a `Sheet`; returned history rows shown.
- **X1** (`/clinic/audit`): full log newest first via `getAuditLog`; per row: what happened (the event's plain-language message) · when · actor as **human label** with the literal enum beside it (the CR-010 exception, X1 only) · patient as **masked name**; three `Select` filters (actor type, event type, date) applied in place; **the proof moment**: filtered to `dose_status_recorded` the list shows exactly سارة's five rows, every actor the agent or the system, called out in an `InlineNotice` in your catalogue's words; empty filter result state; metadata only — no medication list, alert text or dose detail; no export control; `ActivityRow` table layout at 1440.

## ACCEPTANCE
Every state in the five SCREENS rows demonstrated in Playwright at 390/834/1440 × ar/en, axe clean, no overflow · X0's refusal wording equals A1's `no_claims` wording for both an accountless and a patient-only ID (explicit test) · د. خالد sees the chooser, م. دانة never does and lands with no Review tab (explicit test) · G2s commit round-trip: confirm `ia-001` → G1s empty → the audit log gains `alert_reviewed` (through the published API) · G3s confirm clears `needsReview` and the rx starts feeding schedule (assert via `getPrescription`); return sets `returned` with the reason · X1 proof moment as an explicit test (filter → exactly 5 rows → zero non-agent/system actors) · no patient reachable without a queue item (module-graph/call-log assertion) · guards clean · twelve-point checklist per screen.

## INVARIANTS — G1, G10, G12, G9 first, then all twelve verbatim
G1: review decisions write review fields and audit events, **never a dose status** — nothing in this bundle touches `Dose.status`. Rule 6 on X0 as above. Rule 7/G9: the CR-010 exception is exactly bounded — the literal enum appears on X1 only, beside its label; everywhere else (G1s/G2s/G3s included) human labels only; no chat id, token or endpoint anywhere. The admin opens no clinical record: X1 links to nothing clinical.

## PROHIBITIONS
The template's full list: no new dependency · no contract change · no new shared component · no invented seed value or citation · no hard-coded colour/size/copy · no `Date.now()` (waiting time comes from the data layer) · no fetch/mock import · no clinic link added anywhere outside the clinic shell · no clinical body reachable by an admin session · no edit outside your files · no bare `useSearchParams` (D-008 — filter state included; server pages read `searchParams` via props).

## NOTES TO RECORD — `docs/backend-notes/wp4i.md`
§2: the review queue is a mock view; what the backend must precompute (waiting time, severity order). §3: the reviewer's no-free-lookup rule as a server refusal (proving call: fetch a patient context for an alert id not in the queue → 404/403); the admin's no-clinical-read rule; `submitReviewDecision` idempotency/double-commit refusal. §4: fields the five screens blank without. §7: anything awkward (the in-place G1s/G3s switch, the X1 filter model, D-007's sign-out placement if you touched an AppBar).

## REPORT BACK
File list · per-screen state table · whether features/identity offered a reusable sign-in composition (and what you did) · command outputs verbatim (`npm run typecheck`, `lint`, `guards`, `test`, `npx playwright test tests/e2e/clinic.spec.ts --workers=1`) · the proof-moment test result · twelve-point checklist per screen · assumptions · gaps. Nothing committed.
