Per-work-package fragments of `docs/BACKEND-NOTES.md` (D-003): `wp1.md`, `wp3.md`, `bundle-a.md` … `bundle-i.md`.
Each subagent appends only to its own file: the mock shortcuts it relies on (§2), the invariants it
satisfies by leaving a control out (§3), the fields its screens depend on (§4), and anything awkward
(§7). The lead merges them into `docs/BACKEND-NOTES.md` at each gate and deletes nothing here.
