# AP-09 · Telegram linking from the app

Plan section 7.3 (AP-09), finding 2. Owner's answers: D11 (CR-083, the token only in a server
redirect) and D13 (CR-085, the handle from `getMe`). Change requests recorded: CR-086 (the route
and the form), CR-087 (the waiting state).

## What changed

- **One way in.** `POST /api/messaging/telegram/open` (`app/api/messaging/telegram/open/route.ts`)
  replaces F1's `GET` (commit 2a3c901, cherry-picked first, then replaced; `GET` now answers `405`).
  A same-origin form post (the `Origin` host must equal `x-forwarded-host` or `Host`, the check Next
  makes for a Server Action; a missing or `null` Origin, or a `Sec-Fetch-Site` other than
  `same-origin`, is refused) from a patient or an `active` caregiver mints the person's own link
  through `startMessagingLink` and answers `303` to `https://t.me/<bot>?start=<token>`. The token is
  only in that `Location` header. Everything else is a `303` back to the screen with no token. While
  the bot is simulated (no `JURAH_BOT_TOKEN`), the link is minted and the answer goes back to the
  screen; nothing is ever sent to a `t.me` address then. If Telegram does not name the bot
  (`getMe` fails), nothing is minted. `Cache-Control: no-store` and `Referrer-Policy: no-referrer`
  on every answer; `next.config.ts` gains a path rule because the site-wide
  `strict-origin-when-cross-origin` replaced the route's own `Referrer-Policy` (seen in the first
  local curl, below).
- **The link-token rules are unchanged.** `lib/data/pg/channels.ts` is not in the diff: single use,
  15 minutes, one live token per subject, the caregiver trigger.
- **One form.** `features/ambient/TelegramLinkForm.tsx`: the design system's `Button` inside a plain
  `<form method="post">` with two hidden fields, `locale` and `from` (`notifications`, `setup`,
  `profile`; the route maps them to fixed return paths, so there is no open redirect). A real bot:
  `target="_blank"`, so this tab stays and waits. Simulated: it posts in place. Used by E5 (not
  connected, expired, the iOS "chat instead", pending "open again"), A2's Telegram offer and F4.
  No screen calls `startMessagingLink` any more.
- **The page never holds the token.** E5 and F4 pass `linkForScreen(link)` (`lib/messaging/link.ts`)
  to their client components, which drops `linkToken` and `chatId` on the server. Before, the whole
  `MessagingLink` was a client prop, so a pending link's token was in the page's RSC payload (proof
  below).
- **The waiting state (CR-087).** `useLinkConfirmation`: every 3 s for 5 minutes (the token lives
  15), at once when the person comes back to the tab, and on "I pressed Start" (ضغطتُ «ابدأ»), which
  restarts the five minutes and, when the link is still waiting, says so in one quiet
  `role="status"` line. After five minutes the notice says the page stopped checking by itself. E5
  and F4 both; F4's pending row had no action and did not poll at all before.
- **Mock parity with the database.** The mock's `startMessagingLink` now refuses a caregiver who is
  not `active` (the trigger `link_caregiver_must_be_active`), its auto-confirm connects only a
  still-pending row of an active subject (as `connectByToken`), and it spends the token on connect.
- **One mock store per server process.** The first full e2e run failed on حمد's round trip: the
  route answered its 303, and E5 still read "not connected". Next bundles a route handler apart from
  the pages, and `lib/data/mock/store.ts` kept the store in a module-level `let`, so the route minted
  into its own copy. The store now lives on `globalThis`, and live audit and link ids are counted
  from the store (a counter per module copy would issue one id twice).
  `tests/unit/data/mock-store-shared.test.ts` proves both with two module copies. Mock only: the
  database was always shared.
- **Copy.** Four lines in `i18n/copy/ambient.ts` (`e5ChatCheckAction`, `e5ChatOpenAgainAction`,
  `e5ChatStillWaiting`, `e5ChatStoppedChecking`), Fusha, no em dash, each locale its own language,
  `placeholder: true`, reused by F4.
- **Documents.** `docs/API-SURFACE.md` §B lists the route (and says it is the one route a screen
  reaches); `docs/BACKEND-NOTES.md` and `docs/BACKEND-DIVERGENCES.md` D-12 describe the new path.

## Proof

**Merge.** `origin/main` merged in (8 commits: AP-12's CR-091/0014, the D-042 demo-people series, AP-02's
CR-101). One conflict, `docs/DECISIONS.md` (both branches appended at the end) — resolved by keeping
both tails, main's Gate B · AP-02 block first, this branch's Gate B · AP-09 block (CR-086, CR-087)
after, neither block's text changed. Commit `e8812d6`.

```
$ grep -c '^<<<<<<<\|^=======$\|^>>>>>>>' docs/DECISIONS.md
0
$ grep -c '^### CR-08[67] ' docs/DECISIONS.md
2
$ grep -c '^### CR-091 \|^### CR-101 ' docs/DECISIONS.md
2
$ git status -sb
## polish/ap-09-telegram-link...origin/main [ahead 8]
```

**Targeted checks** (`npm run typecheck && npm run guards && npm run notes:check`, run right after
the merge, each its own process, no `next build`):

```
$ npm run typecheck; echo $?
> tsc --noEmit
0

$ npm run guards; echo $?
Jur'ah repository guards
✓ guard 2 · no raw hex / px font-size / px radius in ui code
✓ guard 3 · seam: no mock import outside lib/data, no fetch in ui code
✓ guard 4 · G1: no Dose.status write path, no notification action
✓ guard 5 · logical properties only (no left/right)
✓ guard 6 · G3: REFERENCE_NOW drives every time comparison (no Date.now())
✓ guard 7 · no literal user-facing string outside i18n/copy
✓ guard 8 · SQL runs only through lib/db (withSession is the one path)
✓ guard 9 · no secret in the repository or the client bundle
    · .env.local absent — the value sub-check (d) had NO INPUT — NOT A PASS for (d); (a)-(c) still ran
✓ guard U · strengthMg is displayed as written, never converted
✓ guard P · owed values: 5 missing, 1000 placeholder copy entries
✓ guard T · every spacing utility names a step the theme generates
✓ guard S · seed invariants (docs/Seed Dataset.md)
All guards passed.
0

$ npm run notes:check; echo $?
✓ ## 1. The data-access surface … ## 7. Things Phase 1 found out the hard way
§1 rows for lib/data/api.ts: 50/50 · §5 shapes for lib/data/api.ts: 50/50
§1 rows for lib/session/api.ts: 5/5 · §5 shapes for lib/session/api.ts: 5/5
0
```

All three exit 0. Guard 9's `.env.local absent` note matters below.

**Unit tests**, the six AP-09 files (`npx vitest run tests/unit/api/telegram-open.test.ts
tests/unit/ambient/NotificationsScreen.test.tsx tests/unit/caregiving/f4TelegramLink.test.tsx
tests/unit/caregiving/zeroWriteControls.test.tsx tests/unit/data/mock-store-shared.test.ts
tests/unit/identity/setup-flow.test.tsx`):

```
 Test Files  6 passed (6)
      Tests  63 passed (63)
   Duration  26.14s
```

The redacted `303` proof (`tests/unit/api/telegram-open.test.ts`, re-run alone with `--reporter=verbose`
so the test's own `console.log` prints; the default line reporter used for the six-file run above
does not echo per-test stdout, so this one line is pasted from that second, otherwise-identical run —
same file, same assertions, 12/12 passed both times):

```
[AP-09] 303 Location: https://t.me/jurah_real_bot?start=<token, 43 chars> · Cache-Control: no-store · Referrer-Policy: no-referrer
```

**e2e** (`E2E_PORT=3103 npx playwright test tests/e2e/ambient.spec.ts tests/e2e/identity.spec.ts
tests/e2e/caregiving.spec.ts --reporter=line`, mock backend, `JURAH_DATABASE_URL` unset). It completed
inside the 8-minute budget, it was not killed:

```
  254 failed
  10 skipped
  12 passed (4.6m)
```

**This is an environment gap in this worktree, not an AP-09 regression.** This worktree has no
`.env.local` (guard 9 flags it above) and no `JURAH_SESSION_SECRET` in the shell (owner-supplied per
`.env.example`, D-018). `tests/e2e/helpers/session.ts` throws `JURAH_SESSION_SECRET is not set —
cannot mint a signed session cookie (D-018)` before any page loads, for every test that signs a
session in; 256 of the 254+10+12 = 276 recorded outcomes carry exactly that error (230 directly, 26 a
second call site), and the rest (`toHaveURL`/`aborted`/`element(s) not found`, ~19) are the same
sessions failing further downstream. The 12 that passed are the ones that need no signed session:
route-level CSRF/no-token checks (`E5 — the link route refuses everything but a same-origin post`),
static-render and axe-clean checks on public screens, and the like — which is the same shape of pass
as the earlier, narrower runs this package's `docs/DECISIONS.md` entries describe. No AP-09 fix
changed `tests/e2e/helpers/session.ts` or anything it reads. This blocks confirming items (a)-(d) of
the package acceptance end to end in this environment; the redacted `303` unit test above and the
committed screenshots are the substitute proof available here. Owed: the lead session, running with
`JURAH_SESSION_SECRET` set, re-runs these three specs on the merged tree before merging.

**Not run on this branch:** the full `npm run verify` (typecheck, lint, `check:tokens`, guards,
`test`, `seed:diff`, `notes:check`, `next build`). It does not fit alongside the e2e run in the time
box, both use `.next`. Owed to the merge step (see "Owed, and by whom" below).

## Hallmark audit and the UX checklist

The Hallmark skill's `audit` verb was run on the three screens before editing (read-only, then
fixed inside the design system: `components/ui/*`, the compiled tokens, the boards' layouts;
Hallmark's own themes, fonts and colours do not apply to this repository). The project is
design-system managed (`docs/design-system/`), so the structural-variety rules are inverted: these
screens must share the system, not differ from it.

| Screen | Finding (severity) | Where | Fixed how |
|---|---|---|---|
| E5 `NotificationsScreen.tsx` | Silent wait: after F1's five minutes the poll stopped while the notice still said the page "will update by itself"; no way to check again (major) | pending block | "I pressed Start" re-reads now and restarts the window; after five minutes the notice says it stopped checking; a calm `role="status"` line when a check finds nothing yet |
| E5 | One `useTransition` for the whole screen: a push action put spinners on the chat's buttons and the reverse (major, "spinners that flash" on buttons the person did not press) | `pending` shared by 7 buttons | one busy state per section (`pushBusy`, `chatBusy`); the link form has its own |
| E5 | The pending "Open Telegram" reused the first step's label although it starts a fresh link (minor) | pending block | "Open Telegram again" / «افتح تيليجرام مرة أخرى» |
| E5 | The expired state's retry lacked the `link` icon every other connect action carries (minor) | expired block | `icon="link"` |
| A2 `SetupFlow.tsx` | Three offers drawn equal, but one shared busy flag: tapping Telegram spun "Allow notifications" too (major) | step 1 offers | the Telegram offer is a form with its own state; unit test "one tap never spins another offer" |
| A2 | With a real bot, the tab moves on without saying "press Start in Telegram" (minor) | step 1 → 2 | **not fixed**: a notice the Setup board does not draw; the Telegram tab itself shows Start, and E5 shows the wait. Left for the owner |
| F4 `CaregiverProfile.tsx` | Pending row was a dead end: no control, no poll, "Connecting…" until a manual reload (major; UX §1 "no dead ends") | chat row | the row polls, and carries "I pressed Start" and "Open Telegram again"; the still-waiting line beneath |
| F4 | One busy flag for push, chat and unlink (minor) | whole screen | push has its own |

No finding touched a colour, font, radius or spacing value: every fix uses `Button`,
`InlineNotice`, `MenuRow`'s trailing slot and the existing `type-body-small text-ink-muted` line.

**Hallmark pre-emit scores** (1 to 5): **P4** (one position: the link is the person's own step,
so the screen waits and offers the next step instead of failing) · **H4** (one primary per
screen, proven by a unit test in both push states) · **E4** (focus, 44/48 px targets and the
spinner come from `Button`; the wait is announced politely) · **S4** (Telegram's own word,
«ابدأ», in the button) · **R4** (two controls and one line added, nothing decorative) · **V4**
(shares the design system on purpose; no new component, no new token).

**UX Principles checklist** (docs/UX Principles.md, twelve points), E5 · A2 · F4:

| # | E5 | A2 | F4 |
|---|---|---|---|
| 1 purpose | yes: "Waiting for confirmation" names the state | yes (unchanged) | yes: the row's own state line |
| 2 one primary | yes: "I pressed Start" is primary only when push is decided (unit test) | yes: three equal offers, none primary (unit + e2e) | yes: no primary in the row |
| 3 home and back | yes (unchanged app bar, tab bar) | yes | yes |
| 4 390 px, RTL; 200 % text | 390/834/1440 × ar/en, no overflow (e2e `noOverflowAndAxeClean` on the pending state); 200 % not measured here | 390 × ar (e2e) | 390/834/1440 (existing sweeps); 200 % not measured |
| 5 four states | content / loading / error (`?view=`) / not connected; plus waiting | unchanged | unchanged |
| 6 word or icon, not colour | yes: info icon + title, text line | yes | yes: text |
| 7 targets 44/48, visible controls | `size="lg"` 48 px | 48 px | `Button` md, 44 px |
| 8 errors say what next | the still-waiting line says what to do; no error state added | n/a | same line |
| 9 a control that records a dose | **none**: the new controls re-read the page or open Telegram | none | none |
| 10 fixed vocabulary, no technical id | no token anywhere in the document (e2e `expectNoLinkToken`) | same (e2e) | same (e2e) |
| 11 tracking off, notifications off | calm and complete (حمد, unchanged e2e) | "Later" equal | n/a |
| 12 caregiver: own controls only | n/a | n/a | only the caregiver's own chat and push controls (unit test lists every allowed button) |

## Owed, and by whom

No human-owed value in this package: the handle comes from `getMe` (CR-085), and nothing here
needs a schema change (no migration; `lib/data/pg/channels.ts` is not in the diff).

**Owed by the merge step, before merging** (time-boxed out of this run):

1. The full `npm run verify` on the merged tree (typecheck, lint, `check:tokens`, guards, `test`,
   `seed:diff`, `notes:check`, `next build`) — not run here; see "Proof" above.
2. `tests/e2e/ambient.spec.ts`, `identity.spec.ts` and `caregiving.spec.ts`, with
   `JURAH_SESSION_SECRET` set (`.env.local`, D-018) so the signed-session tests actually exercise the
   app instead of failing at cookie-minting — this run's `254 failed` is that missing value, not a
   code regression; see "Proof" above.
3. The iPhone Home Screen (standalone) behaviour of the `target="_blank"` form, where cookies may
   differ and the route may send the person back to sign in. Not observed here, so no fix was built.
   Owed: the lead session observes it on a device; the owner (Mohammad, on Hamad's delegation)
   decides the fix.
4. A2 (`SetupFlow`) has no "press Start in Telegram" notice after the tab moves on with a real bot.
   Left unfixed because the Setup board does not draw it (see the Hallmark table above). Owed: an
   owner design decision.
5. The 200% text-size check on E5 and F4 was not measured. Owed: the lead session's UX sweep (AP-14).
6. Retiring the `REAL_BOT_HANDLE` `[TO BE SUPPLIED]` marker in `lib/config.ts`, which nothing reads
   now that the handle comes from `getMe`. Owed: a documents decision in AP-19; guard P's count stays
   unchanged here.

**Live steps for the lead, after merge** (none was done here; no live system was touched):

1. On production (the bot token is configured there, so `BOT_IS_SIMULATED` is false), as one of
   the two D-041 demo patients (CR-080), open E5 and press «افتح تيليجرام». Expect a new tab on
   `t.me/<the bot getMe names>?start=…`, Telegram opening the bot, and after Start the E5 tab
   turning from «بانتظار التأكيد» to «مربوطة منذ …» within about 3 s (no reload). This is J1 in
   AP-14.
2. Before pressing Start, press «ضغطتُ «ابدأ»» once: the calm still-waiting line appears, no error.
3. As that patient's active caregiver, the same from F4. If the journey also revokes that caregiver
   (on the D-041 demo pair only, reset afterwards, CR-080), F4's «ربط تيليجرام» must mint nothing.
4. Read the 303 on production once with the browser's network panel (not curl: that needs a
   session cookie): `Location: https://t.me/<bot>?start=<43 characters>`, `Cache-Control:
   no-store`, `Referrer-Policy: no-referrer`. Do not paste the token anywhere.
5. On an iPhone, from the Home Screen app: a `target="_blank"` form leaves the standalone app for a
   browser view whose cookies may not be the app's, so the route may answer "back to sign in". If it
   does, the fix is the owner's call (for example, posting in place when `display-mode:
   standalone`); it is not built here because it was not observed.
6. AP-13 re-reads the webhook with `getWebhookInfo` (CR-085).
7. `REAL_BOT_HANDLE` in `lib/config.ts` still holds `[TO BE SUPPLIED]` and is read by nothing (the
   handle comes from `getMe`); retiring the marker is a documents decision for AP-19, not made here,
   so guard P's count is unchanged.

## Update from the merge gate (2026-09-24, PR #11)

Closes owed items 1-2 above (the full `npm run verify` and the e2e specs with a session secret set).
Items 3-7 are unchanged, still owed to the parties named.

**Merge.** `git fetch origin && git merge origin/main` (15 commits ahead: AP-12's audit policy,
AP-06's DDInter rebuild, AP-03's extraction core, AP-08's eval harness, AP-02's voice fix, each
already merged to `main` through its own PR). Auto-merged, no conflict markers in `docs/DECISIONS.md`
or `agents/scripts/build.js`:

```
$ git merge origin/main --no-edit
Auto-merging docs/DECISIONS.md
Merge made by the 'ort' strategy.
 39 files changed, 2593 insertions(+), 304 deletions(-)
```

The merge concatenated main's appended `docs/DECISIONS.md` sections (CR-091, 096, 097, 098, 099, 101)
ahead of this branch's own CR-086/087, leaving the file out of CR-number order (the same shape of
issue AP-03's own gate hit and fixed the same way). Fixed in a follow-up commit,
`Reorder Gate B DECISIONS.md sections into CR-number order`: the "Gate B - AP-09" section (header,
CR-086, CR-087) moved to sit right after the D9-D13 answers and CR-085, ahead of CR-091 - a pure
relocation (`git diff` shows one deletion hunk and one insertion hunk of identical text, plus the two
`---` separator lines the move needs to match the file's own convention); no entry's text changed.

`cd agents && JURAH_API_BASE=https://tryjuraaah.vercel.app/api/agent npm run build` regenerated the
five workflow JSON files (`agents/workflows/agent-*.json`); the diff against the committed copies was
empty under `git diff --ignore-cr-at-eol` (the build script emits LF, the repository's checked-in
copies are CRLF) - Windows line-ending noise only, so those five files were restored with `git
restore` and not committed, and `cd agents && JURAH_API_BASE=... npm run verify` read `all checks
passed` (`agent-telegram-inbound`, `agent-interaction-screening`, `agent-checkin-daily`, `agent-alexa`,
`agent-webchat` all green).

**Root `npm run verify`, full, twice.** First run failed at `check:tokens` only:

```
> jurah-frontend@0.1.0 check:tokens
> tsx scripts/build-tokens.ts --check

✗ styles/tokens.css is stale against docs/design-system/tokens.json — run npm run build:tokens
VERIFY_EXIT_CODE:1
```

The Windows-checkout-vs-generator line-ending gap this package's own run instructions name (LF
generated, CRLF checked in) - `npm run build:tokens` (confirmed `git diff --ignore-cr-at-eol` on
`styles/tokens.css` was empty, so no content actually changed), then the full gate again, clean:

```
> jurah-frontend@0.1.0 check:tokens
✓ styles/tokens.css is in sync with docs/design-system/tokens.json

> jurah-frontend@0.1.0 guards
Jur'ah repository guards
✓ guard 2 · no raw hex / px font-size / px radius in ui code
✓ guard 3 · seam: no mock import outside lib/data, no fetch in ui code
✓ guard 4 · G1: no Dose.status write path, no notification action
✓ guard 5 · logical properties only (no left/right)
✓ guard 6 · G3: REFERENCE_NOW drives every time comparison (no Date.now())
✓ guard 7 · no literal user-facing string outside i18n/copy
✓ guard 8 · SQL runs only through lib/db (withSession is the one path)
✓ guard 9 · no secret in the repository or the client bundle
✓ guard U · strengthMg is displayed as written, never converted
✓ guard P · owed values: 5 missing, 1000 placeholder copy entries
✓ guard T · every spacing utility names a step the theme generates
✓ guard S · seed invariants (docs/Seed Dataset.md)
All guards passed.

> jurah-frontend@0.1.0 test
> vitest run
 Test Files  123 passed (123)
      Tests  1192 passed | 1 skipped (1193)

> jurah-frontend@0.1.0 seed:diff
✓ seed diff — every record present at the stated values, no extra record

> jurah-frontend@0.1.0 notes:check
✓ ## 1 … ## 7 (all sections present)
§1 rows for lib/data/api.ts: 50/50 · §5 shapes for lib/data/api.ts: 50/50
§1 rows for lib/session/api.ts: 5/5 · §5 shapes for lib/session/api.ts: 5/5

▲ Next.js 16.3.5 (Turbopack)
✓ Compiled successfully in 28.9s
✓ Finished TypeScript in 23.4s
✓ Generating static pages using 11 workers (74/74) in 1642ms
Route (app)
… /api/messaging/telegram/open  ƒ  (Dynamic)  …
VERIFY_EXIT_CODE:0
```

`styles/tokens.css` restored with `git restore` afterward (the `build:tokens` run, not a real content
change) and not committed.

**e2e, three attempts.** `E2E_PORT=3103 npx playwright test tests/e2e/ambient.spec.ts
tests/e2e/identity.spec.ts tests/e2e/caregiving.spec.ts` with a fresh local `JURAH_SESSION_SECRET` (a
throwaway 32-byte hex value generated in the same shell call, never printed or committed), mock
backend, `JURAH_DATABASE_URL` unset, matching the previous session's proof that the redirect and the
303/no-token/no-store/no-referrer behaviour already pass at the unit level.

*Attempts 1 and 2 (the full three files, 276 tests, 6 then 2 workers) did not reach a clean exit.*
This machine was running several other packages' gates at the same time (verified with
`Get-CimInstance Win32_Process`, not assumed): a separate `next dev -p 3110`, `-p 3104` and `-p 3113`
each bound to another worktree, a separate `cd .../ap-10 && npm run verify` mid-run, and an `ap-11a`
diagnostic script curl-bursting fifteen routes on its own `next dev -p 3113` - eleven to eighteen
`next dev`/`next build`/`vitest` processes at once, on top of this run's own. At 6 workers: 231/276
tests started before the 12-minute mark, 169 numbered failures, spread across nearly every screen in
`ambient.spec.ts` (E1 through E5), with the webServer logging the same
`SyntaxError: Unexpected non-whitespace character after JSON at position 3626` and `Error: aborted
(ECONNRESET)` between almost every test - a transport symptom, not a per-test one. Killed
(`taskkill //F //T //PID <the real playwright cli.js tree, found via Win32_Process since Git Bash's
own `ps` does not show it>`) once the pattern was clear (broadly, not two or three, failing). At 2
workers, retried on the theory that less of *this run's own* concurrency would help: 73/276 started,
7 numbered failures, all in `caregiving.spec.ts`'s F0/F1 (the consent screen and the caregiver invite
form) - screens this package does not touch - with no further `SyntaxError` noise. Killed again after
confirming the failures were not in this package's own screens, rather than spend the remaining time
box on files outside AP-09 under a contention level this session cannot control.

*Attempt 3, the acceptance-relevant tests only, isolated and clean.* `--grep "Telegram" --workers=1`
against the same three files - every test this package's acceptance criteria actually names (F4's
three caregiver-invitation states and A2's Telegram offer, across phone/tablet/desktop):

```
Running 12 tests using 1 worker
[1/12] … F4 … an ACTIVE caregiver (سارة, cg-02): "Connect Telegram" … it connects; no token on the page
[2/12] … F4 … a REVOKED caregiver (طلال, cg-06) … the same form mints nothing; the row stays not connected
[3/12] … F4 … a pending-only invitee (ناصر, cg-03) … gets no link and no token
[4/12] … A2 … the Telegram offer posts a form … no token is ever on the page
 (repeated at [5-8] tablet-834, [9-12] desktop-1440)
  2 skipped
  10 passed (1.2m)
E2E_EXIT_CODE:0
```

Every one of these same twelve tests was among the ones that failed at 6 workers in attempt 1 (proof
that the earlier failures were this machine's concurrent load, not this package's code): isolated from
that load, all ten runnable cases pass and the other two skip as designed (viewport tags), with zero
`SyntaxError`/`aborted` noise. This is the caregiver-active-only e2e item (d) plus the A2 offer item
(b) proven directly; it is not a substitute for a clean full-suite run, and is recorded as exactly
that - not claimed as one.

**Not achieved in this run:** a single clean, uninterrupted exit-0 of the exact three-file command
across all 276 cases. Owed to whichever session runs this gate on a machine not sharing CPU with
several other packages' builds at once, or to a rerun once those finish.

**not-deployed check.** `git diff $(git merge-base HEAD origin/main)..HEAD -- . ':!docs' | xargs grep
-l "not-deployed"` → no matches.
