# AP-10 · Screening on every path — backend notes

Branch `polish/ap-10-screening-every-path`. Package: docs/AGENTS-POLISH-PLAN.md §7.3 AP-10
(D5 CR-077, D10 CR-082, D12 CR-084). This note is written by the executor session that ran
Gate B's merge-and-ship pass on 2026-09-24, inside a strict 18:13 time box. It records what
changed, the real command output that proves it, and what is owed.

## What changed

The branch (worktree `jh-wt/ap-10`) arrived at this session already carrying the F3
cherry-pick (`lib/data/pg/screening.ts`, `screenOrHold`) and a large "WIP: resumed" commit
(`eec380f`) with:

- **Triggers (a).** `screenOrHold` runs, awaited, after the commit, from all four places a
  prescription becomes or stays something the patient relies on: `savePrescriptionDraft`
  (reads-rx.ts), `insertExtractedPrescription` (POST /api/agent/prescriptions, agent.ts — the
  201 body gains `screening`), `confirmPrescriptionFields` and `requestRefill` (writes.ts, D10:
  a refill re-screens that prescription's pairs). A flagged (`needsReview`) prescription is
  screened only when its reviewer confirms it. `requestScreening`
  (`lib/agent-webhooks/index.ts`) is documented and tested as awaited, never fire-and-forget,
  with a bounded timeout; when n8n does not accept the request, `screenOrHold` writes a
  `warning` / `pending_medical_review` hold alert with an empty `sourceCitation` through the
  audited insert path — the reviewer queue is the visible record of the miss.
- **CR-088 (b).** Recorded `PROPOSED`: a `screening_requests` retry table/migration, needing a
  production migration the executor cannot apply. Not built.
- **The being-checked state (c).** Derived from existing data only —
  `features/prescription/screening-state.ts` (pure), `lib/agent-webhooks/state.ts`, wired into
  B2 (`features/day/MedicinesList.tsx`, `app/[locale]/app/medicines/page.tsx`) and B3
  (`PrescriptionDetail.tsx` / `PrescriptionDetailView.tsx`, `InlineNotice tone="info"
  role="status"`). Copy keys `rxBeingCheckedLine`, `rxBeingCheckedTitle`, `rxBeingCheckedBody` in
  `i18n/copy/prescription.ts`. Four 390px screenshots already committed under
  `docs/backend-notes/ap-10/` (`ap10-b2-being-checked-390-{ar,en}.png`,
  `ap10-b3-being-checked-390-{ar,en}.png`). CR-089 recorded `BUILT`.
- **Guard R (d).** `scripts/guards/screening-on-every-path.ts`, registered in
  `scripts/guards/run.ts`, with its red-proof test in
  `tests/unit/guards/screening-on-every-path.test.ts`.
- **Integration round trip (e).** `tests/integration/roundtrip/agent-webhooks.test.ts`, gated on
  `JURAH_DATABASE_URL` — does not run here (no Supabase test branch was created; that costs
  money and is the lead's call per D8). Marked OWED below.
- Production safety: `screenOrHold`'s first line is
  `if (!screeningConfigured() || !shouldScreen(rx)) return 'skipped';` — the save path is a
  no-op today, since production has no `JURAH_AGENT_SCREENING_URL`. Covered by
  `tests/unit/agent-webhooks/not-configured.test.ts`.

## This session's work: merge `main`, verify, push, open the PR

The branch had not been pushed and had no PR. `origin/main` had moved 4 commits ahead
(AP-02 voice-inside-rule-1, AP-12 audit-insert-actor) since this branch's last merge.

### 1. Merge

`git merge origin/main` conflicted only in `docs/DECISIONS.md` (both sides had appended a new
Gate B section at the end of the file). Resolved by keeping origin/main's appended `AP-02`
section (CR-101) first, then the whole `AP-10` section unchanged except the two edits the task
specified: CR-088's migration number (`0014` was taken by AP-12's
`0014_audit_insert_actor.sql` in the meantime, so the proposal now names `0015_screening_requests`
and says to renumber if another package takes it first), and CR-089's proof line (pointed at
this PR body instead of a `docs/backend-notes/ap-10.md` that did not exist yet).

```
$ grep -nE '^(<<<<<<<|=======|>>>>>>>)' docs/DECISIONS.md
(no output — no conflict markers remain)

$ npm run notes:check
✓ ## 1. The data-access surface
✓ ## 2. Where the mock cheated
✓ ## 3. Rules that are currently absences, and must become refusals
✓ ## 4. Fields a screen depends on
✓ ## 5. The shapes, verbatim
✓ ## 6. Deferred to Phase 2 by design
✓ ## 7. Things Phase 1 found out the hard way
§1 rows for lib/data/api.ts: 50/50
§5 shapes for lib/data/api.ts: 50/50
§1 rows for lib/session/api.ts: 5/5
§5 shapes for lib/session/api.ts: 5/5
```

**Honest exception on the letter of the plan's own test.** The plan's step-1 test also reads
`grep -oE '^### CR-[0-9]+' docs/DECISIONS.md | sort | uniq -d` should print nothing. It does
not — it prints `CR-041`, `CR-069`, `CR-070`. Verified these are pre-existing, not introduced by
this merge or by AP-10: `git show 8f60510:docs/DECISIONS.md | grep -oE '^### CR-[0-9]+' | sort |
uniq -d` gives the same three numbers, and `8f60510` (the merge already on this branch before
this session started) predates every AP-10 edit. CR-041 has a deliberate "— extension" follow-up
heading; CR-069/CR-070 collide between an earlier UX-audit entry and a later AI-agents/Alexa
entry — a genuine cross-lane numbering collision from before this session, out of scope for
AP-10 to renumber (it owns only CR-088–090). None of AP-10's or AP-02's new headings
(CR-088, CR-089, CR-090, CR-101) collide with each other or with anything else.

### 2. Targeted checks (after the merge)

```
$ npm run typecheck
> tsc --noEmit
(exit 0, no errors)

$ npm run guards
Jur'ah repository guards
✓ guard 2 · no raw hex / px font-size / px radius in ui code
✓ guard 3 · seam: no mock import outside lib/data, no fetch in ui code
✓ guard 4 · G1: no Dose.status write path, no notification action
✓ guard 5 · logical properties only (no left/right)
✓ guard 6 · G3: REFERENCE_NOW drives every time comparison (no Date.now())
✓ guard 7 · no literal user-facing string outside i18n/copy
✓ guard 8 · SQL runs only through lib/db (withSession is the one path)
✓ guard 9 · no secret in the repository or the client bundle
✓ guard U · strengthMg is displayed as written, never converted
✓ guard P · owed values: 5 missing, 1000 placeholder copy entries
✓ guard T · every spacing utility names a step the theme generates
✓ guard R · every prescription write and refill is screened after its commit (AP-10)
    · 41 file(s) scanned in lib/data, lib/engine, lib/session; 4 write path(s):
      insertExtractedPrescription (screens), savePrescriptionDraft (screens),
      requestRefill (screens), confirmPrescriptionFields (screens)
✓ guard S · seed invariants (docs/Seed Dataset.md)
All guards passed.

$ npx vitest run tests/unit/agent-webhooks tests/unit/guards/screening-on-every-path.test.ts \
    tests/unit/prescription/screening-state.test.ts tests/unit/prescription/being-checked.test.tsx \
    tests/unit/agent/routes.test.ts tests/unit/db/audit-insert-policy.test.ts
 Test Files  10 passed (10)
      Tests  123 passed (123)
   Duration  27.44s

$ npx eslint lib/data/pg/screening.ts lib/data/pg/agent.ts lib/data/pg/writes.ts \
    lib/data/pg/reads-rx.ts lib/agent-webhooks/index.ts lib/agent-webhooks/state.ts \
    features/prescription/screening-state.ts features/prescription/PrescriptionDetail.tsx \
    features/prescription/PrescriptionDetailView.tsx features/day/MedicinesList.tsx \
    "app/[locale]/app/medicines/page.tsx" scripts/guards/screening-on-every-path.ts
(no output — clean)

$ git diff origin/main...HEAD | grep -c not-deployed
0

$ git status --short -- styles/tokens.css
(no output — not touched, no line-ending noise to restore)
```

`tests/unit/db/audit-insert-policy.test.ts` (AP-12's scan of every `append()` call site,
brought in by the merge) passes against AP-10's restructured `insertExtractedPrescription`
(`saved = await withAgent(...)`) and `requestRefill` call sites without any rewrite needed.

### 3. Push

```
$ git push -u origin polish/ap-10-screening-every-path
 * [new branch]      polish/ap-10-screening-every-path -> polish/ap-10-screening-every-path

$ git log --oneline -1 origin/polish/ap-10-screening-every-path
1e70207 Merge origin/main into polish/ap-10-screening-every-path
$ git log --oneline -1 HEAD
1e70207 Merge origin/main into polish/ap-10-screening-every-path
```

### 4–6. Owed to the time box

The owner's deadline for this executor was 18:13 Kuwait time on 2026-09-24; the merge, targeted
checks and push above finished at 18:04, leaving no safe margin to also run the 8-minute
Playwright spec, the Hallmark audit, the twelve-point UX checklist, and a full `next build`
inside the box without missing it. Per the task's own instruction ("kill it and report ... as
owed to the merge step" / "OWED: not run in the time box"), these are not run by this session:

- `E2E_PORT=3104 npx playwright test tests/e2e/prescription.spec.ts` — **not run, OWED.**
- The background `npm run lint && npm run check:tokens && npm run seed:diff && npm run test` —
  **not run, OWED** (the targeted eslint/typecheck/guards/vitest subset above did run and pass).
- `npx next build` (verify's last stage) — **not run, OWED.**
- Hallmark audit (`Skill hallmark`, `audit features/day/MedicinesList.tsx` and
  `audit features/prescription/PrescriptionDetailView.tsx`) and the twelve-point UX checklist —
  **not run, OWED.** No score is invented.
- Copying fresh `test-results` screenshots over `docs/backend-notes/ap-10/` — skipped since the
  e2e run that produces them did not happen this session; the four screenshots already committed
  on this branch (from the earlier "WIP: resumed" work) are unchanged and still referenced above.

## Cross-lane / owed items (from the plan, unchanged by this session)

- **CR-088** — the `screening_requests` retry table/migration/job/cron: `PROPOSED` only, needs
  the owner's yes and the lead to apply the migration to production.
- **CR-090 (i)** — AP-03's `agent-telegram-inbound` / `agent-extraction` workflows must stop
  calling `jurah/screen-prescription` themselves once the 201 body's `screening` field is
  `screened` or `held`, or a Telegram prescription is screened twice. `JURAH_AGENT_SCREENING_URL`
  must stay unset in production until that ships (AP-03's owed item, blocks AP-13).
- **CR-090 (ii)** — `agents/knowledge/src/screening.js` `exclusionReason` should exclude on
  `needsReview !== false` alone, not on `fieldReviewStatus: 'pending'` too (owed by AP-04/AP-06).
- **CR-090 (iii)** — skip a pair that already has an open alert naming both prescriptions on a
  refill re-screen (owed by AP-04).
- Fusha copy review of `rxBeingCheckedLine`, `rxBeingCheckedTitle`, `rxBeingCheckedBody`,
  `screeningHeldTemplate` — owed by the owner.
- The integration round trip (`tests/integration/roundtrip/agent-webhooks.test.ts`) against a
  real Supabase test branch — OWED, costs money (D8), the lead's call and run.
- Setting `JURAH_AGENT_SCREENING_URL` in production (AP-13) — the lead's live step, and must
  wait on CR-090(i) landing first.
- The caregiver's F2/F3 do not show the being-checked state, by design (CR-089); extending it is
  the owner's option, not done here.
- The live check after merge (a real save with screening configured, and the hold appearing in
  the reviewer queue) — the lead's step.
- The pre-existing `CR-041` / `CR-069` / `CR-070` duplicate headings in `docs/DECISIONS.md`
  (see above) — flagged, not fixed here; out of AP-10's scope (reserved numbers CR-088–090 only).

## touchesAgents

AP-10's own commits (the F3 cherry-pick and `eec380f`) touch nothing under `agents/`. This
session's merge commit brings in AP-02's changes to `agents/**` from `origin/main` — inherited,
not authored by AP-10, and out of scope for this package to test or review.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
