# AP-00 · Baseline (agents polish plan)

Run on `1b533d3` (the plan's merge commit; `d138b1a` plus `docs/AGENTS-POLISH-PLAN.md`), 2026-09-24, by the lead session on Mohammad's machine. Every number below is the pasted output of the command next to it.

| Measure | Plan (section 3) | Measured | Command |
|---|---|---|---|
| `agents` tests | 74 | **74 pass / 0 fail** | `cd agents && npm test` |
| `agents` workflow checks | pass | **all checks passed** | `cd agents && npm run check` |
| `agents/knowledge` tests | 65 | **65 pass / 0 fail** | `cd agents/knowledge && npm test` |
| `agents/knowledge` workflow checks | pass | **all checks passed** | `JURAH_API_BASE=https://tryjuraaah.vercel.app/api/agent npm run check` |
| Seed ingredients the DDInter index covers | 2 of 9 | **2 of 9** (Levothyroxine, Calcium carbonate); index 11 drugs, 19 pairs, built 2026-09-20 | `cd agents/knowledge && npm run coverage` |
| Root agent unit tests | 129 (`tests/unit/agent`) | **168 pass in 9 files** (the plan's command also includes `tests/unit/agent-webhooks` and `tests/unit/assistant`) | `npx vitest run tests/unit/agent tests/unit/agent-webhooks tests/unit/assistant` |
| Repository gate | - | **exit 0**: all guards passed · 118 files, 1139 passed, 1 skipped · seed diff clean · build compiled | `npm run verify` |
| `[TO BE SUPPLIED]` markers in `agents/` | 5 | **5** | `grep -rhoE 'TO BE SUPPLIED\|TO_BE_SUPPLIED' agents \| wc -l` |

## The n8n workflow list (live, read with the n8n REST API in Mohammad's logged-in browser)

| Workflow | Id | Active |
|---|---|---|
| agent-telegram-inbound | cKrnBf3qzSGyFE1r | yes |
| agent-checkin-daily | IW8wX3pDFYa8ezvY | yes |
| agent-interaction-screening (legacy) | bMVDCQbSwUtMHJ4x | yes |
| agent-alexa | qavdr94aZH6dYZ4M | yes |
| agent-webchat | QmQkIbPsCZNcgEqK | yes |
| agent-adherence-inapp, agent-rescheduling, agent-adherence-inbound, agent-adherence-outbound, agent-orchestrator | y3ULglvaHO4sssYH, PBmmUzP1mhGDtewZ, WVwsQLwVFgeC2VHV, kFog1OjoH3L6o1YU, 9Ea3V9WFX6B88R8B | no (superseded 2026-09-23, kept, not deleted) |
| bench-voice-models (test only) | mU0K9t8KUFZ5R2IC | no, archived (the voice-model benchmark of 2026-09-24) |

**Not deployed:** the three drug-knowledge workflows committed in `agents/knowledge/workflows/` (`agent-extraction`, `agent-travel-check`, `agent-interaction-screening-ddinter`). The production app therefore runs the CR-049 stubs for the drug check, prescription intake and screening (no `JURAH_AGENT_*_URL` for them, per the environment list known to the operator; to be confirmed with the Vercel connector in AP-13).

## Connectors

The n8n connector is not installed in this session; the plan's fallback (the n8n REST API, run in Mohammad's browser) is used, as section 4 allows. The Supabase connector reaches `frvubflbpujwuhsxweue`. The Vercel connector has no access to the `coded-plan` team; Vercel checks go through the dashboard in Mohammad's browser.
