# AP-11 · A real Orchestrator, and Travel Check from Telegram

Branch `polish/ap-11-orchestrator`, from `origin/main` at `c53ca22`. Change requests: `docs/DECISIONS.md` CR-094 (the Orchestrator's own choices) and CR-095 (the agent-side `cannot_verify` app outcome, the other half of D6/CR-078). Both recorded as *Accepted by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end* - no production schema change, no owed value invented.

## What is built

1. **`agents/lib/orchestrator.js`** (new, with `agents/test/orchestrator.test.js`, 25 tests): `routeInbound(payload, {store, now})` (`adherence` | `extraction` | `photo` | `travel` | `reply` | `null`); `photoQuestion(mime, buffer)` and `decidePhoto(res)` (the floor `MIN_ROUTE_CONFIDENCE = 0.7`); `choiceButtons`/`parseRouteTap` (`or:rx:<messageId>` / `or:box:<messageId>`, both well under Telegram's 64-byte callback-data limit); `rememberPhoto`/`takePhoto` (a one-shot, 24-hour, ~100-entry-capped pending choice in `$getWorkflowStaticData('global')`, keyed by patient id + message id, holding only `{fileId, kind, time}`); `caregiverReply`/`orchestratorReply`; `travelBody`/`travelReply` (one fixed line per Travel Check verdict). No model-contract marker: `PHOTO_PROMPT`/`PHOTO_SCHEMA` are built into the vision request by plain Code, the same way `agents/knowledge/src/extraction.js` carries its own `PROMPT`/`RESPONSE_SCHEMA` - there is no chain node here to carry them instead.
2. **`agents/scripts/build.js`**: `route (deterministic)` now calls `routeInbound`; the existing `a prescription photo?` IF is unchanged, and its false branch runs three new chained IFs (`photo?`, `medicine box?`, `fixed reply?`, the existing `ifNode` helper, no Switch node). New nodes: `orchestrator: download the photo` → `orchestrator: ask what the photo is` → `Gemini: what is this photo?` (HTTP `generateContent` on `VISION_MODEL_PATH`, the existing `googlePalmApi` credential type, `neverError`, 30 s) → `orchestrator: decide (deterministic)` (remembers an unsure photo), then two more IFs to `prescription (photo)?` (the *existing* `Telegram: download the file`, re-download, extraction unchanged) and `a medicine box (photo)?` (`Telegram: download for travel check` → `orchestrator: build the travel request` → `n8n: travel check`, POST `N8N + '/jurah/travel-check'`, the existing header-secret credential type, `neverError`, 60 s → `orchestrator: travel reply (deterministic)`). `other`/`unsure`/a file problem/a caregiver message/an expired tap all reach the one `orchestrator: reply (deterministic)` (it reads `$input`, not a named node, because two different nodes feed it) → `with two buttons?` → the new `telegramTwoButtons` helper (the three-button helper is untouched) or plain text. Every non-adherence tap is closed by one shared `Telegram: close the photo tap`, fanned out **in parallel** with the send (not chained after it, the way the existing adherence-only `was it a tap?` → `Telegram: close the tap` is) - a Telegram send node's own output is not the same item as its input, so a later node cannot read `callbackQueryId` off `$json` post-send without a named back-reference, and here two different Code nodes can feed the send; reading `$json.callbackQueryId` in parallel, straight off whichever Code node just ran, sidesteps that instead of guessing which name to reference.
3. **`agents/scripts/check.js`**: `runner(wf)` now stubs `$getWorkflowStaticData` (one in-memory object per scenario) as the Code node's third constructor parameter, matched in the static "every Code node compiles" check. The three existing extraction scenarios are re-pointed through the new `photoClassifiedAs(kind, confidence, payload, mime, buffer)` helper (route → the photo branch → classified → re-download), so they now prove the Orchestrator's own decision reaches the *unchanged* extraction path; TC-EX-04's second line is exactly what the plan asked - the Orchestrator says `prescription`, extraction itself still says `isPrescription:false`, nothing is saved. New scenarios: a medicine-box photo builds `{patientId, imageBase64, mimeType, language}` and Travel Check's own answer (danger+`alertId`, `cannot_verify`, HTTP 500, `mustEscalate`) each reads back its one fixed line, never an all-clear; `other` gets the fixed explanation with no further download; an unsure photo (0.55) asks with two buttons (`or:rx:777`/`or:box:777`, both ≤ 64 bytes), is remembered, `or:box:777` resumes it for Travel Check with the *same* stored file id, and a second tap on it has expired; a PDF document is decided `prescription` from its mime type alone (`mimeShortcut`), never asked to the model; TC-AD-14 is re-pointed - a caregiver's text, tap, photo and document all get a fixed reply via the Orchestrator, with no doses GET, no model call, no write, no download and nothing remembered (`r.staticData` stays `{}`). A static graph-walk check confirms, by walking `wf.connections` from `orchestrator: reply (deterministic)`, that the caregiver/reply path calls no `httpRequest` node and downloads no file, and, walking from `photo?`, that no node on the whole photo lane names a `/doses/` or `/schedule/` URL (the same quote-anchored pattern the existing Alexa call-assertion already uses, so a documentation mention of the adherence API surface in a shared doc comment is never mistaken for a real call).
4. **`agents/eval/workflow.js` `runCode`**: gained the same `$getWorkflowStaticData` stub (a `store` object threaded across calls) and a `binary`/`buffer` passthrough, so a Code node that reads `$input.first().binary` (as `orchestrator: ask what the photo is` does) can be run by the harness at all.
5. **`agents/eval/sets.js` `routing()`**: wired to the committed nodes. Text/tap/start items run `route (deterministic)` alone; a photo or document also runs `orchestrator: ask what the photo is` → the injected transport on `Gemini: what is this photo?` → `orchestrator: decide (deterministic)`, then maps the outcome to the set's own labels (`adherence`, `extraction`, `travel_check`, `link`, `clarify`, `caregiver_reply`, `other_reply` - newly added to `routing.schema.json`'s enum and the README's row). An `inactive_caregiver` item is never run at all: the app's own relay (`lib/agent/inbound.ts` `subjectForChat`) decides it, scored by that one rule and labelled `detail.decidedBy: 'relay'`. A failed vision call is an `error`, never scored as a route. `agents/test/eval-harness.test.js`'s old "no Orchestrator yet" test is replaced by a measured synthetic smoke set (an injected transport answering `unsure`, scoring 15/15) and a second test proving a failed photo model call is an ERROR, not a route; the committed-empty-sets acceptance test is untouched (the dataset itself stays `[TO BE SUPPLIED]`, so it still prints `NOT MEASURED: routing has 0 of 15 items`).
6. **D6/CR-078's agent half** (`agents/knowledge/src/travel-check.js`): `appOutcomeFor(VERDICT.CANNOT_VERIFY)` now returns `{ kind: 'cannot_verify' }` instead of mapping into `could_not_identify`; `needs_confirmation` and `could_not_identify` are unchanged. Three asserts in `agents/knowledge/test/travel-check.test.js` and one in `agents/knowledge/scripts/check.js` (the AP-06 Ezetimibe × Amlodipine case) now expect it; the README row and the root `tests/unit/agent/knowledge-contract.test.ts` shape test now allow `cannot_verify` alongside `could_not_identify`, both with exactly the key `['kind']`.
7. `agents/README.md`: the Orchestrator's own branch in the diagram, the live-binding notes (`Gemini: what is this photo?` uses the *same* Google Gemini credential as `Gemini: read the prescription`; `n8n: travel check` uses the *same* header-secret credential as `n8n: screen the new prescription` - no new n8n credential is needed), and a warning that any live export of `agent-telegram-inbound` must have its `staticData` stripped before it is shared (it holds pending-choice Telegram file ids).

## One deliberate choice, recorded rather than left silent

`routeInbound` cannot tell an image sent as a Telegram *document* from a PDF sent as a document, because the relay (`lib/messaging/telegram.ts` / `lib/agent/inbound.ts`) forwards a bare `documentFileId` with no mime type at all (confirmed by reading both files; no app change is needed or made here). So a fresh `documentFileId` always enters the same `'photo'` route as an inline photo, and `photoQuestion(mime, buffer)` - run immediately after the file is downloaded, *before* any model call - decides a PDF is `prescription` outright from its mime type (`mimeShortcut`), never spending a Gemini call on it. This reaches the same place the plan's step 2 rule 3 asks for ("a patient's PDF goes straight to extraction"), one hop later than a literal reading of "decided before download" might suggest. Proven by `agents/test/orchestrator.test.js` ("a PDF sent as a document is decided as prescription without asking the model at all") and `agents/scripts/check.js` ("a PDF sent as a document is decided prescription from its mime type alone, never asked to the model").

**One thing the root gate found, fixed here (outside this package's own file list).** Running the full root `npm run verify` surfaced a real cross-package gap: `tests/unit/agent-webhooks/core.test.ts` ("every appOutcome the real agent produces is read back unchanged") runs the real, now-changed `travelCheck` and asserts `readDrugCheck` (`lib/agent-webhooks/core.ts`) returns every outcome byte-for-byte - it did not know about `cannot_verify` yet, since `readDrugCheck` itself is PR #13/AP-11a's file to update, not this package's. The test is updated to assert the documented fallback (`could_not_identify`, matching the *next* test in the same file, "anything unexpected is could_not_identify") for `cannot_verify` specifically, and byte-for-byte unchanged for every other outcome, with a comment naming PR #13 as the closer. Left alone, this would have left the root gate red for a reason CR-095 already called safe; fixed narrowly instead of reported and walked away from. `npx vitest run tests/unit/agent-webhooks/core.test.ts` -> 19/19 pass.

**One thing this branch does not change, on purpose.** `agents/knowledge/scripts/build.js`'s `TC_CHECK` (Travel Check's own generator, outside this package's reserved files) already answers a backend-unreachable profile with a hardcoded `{ verdict: 'could_not_identify', reason: 'profile_unavailable', appOutcome: { kind: 'could_not_identify' } }` - a different claim from `VERDICT.CANNOT_VERIFY` ("we know the profile, this specific pair is outside our data"): here we do not even know the profile. Its test in `agents/knowledge/scripts/check.js` ("profile unreadable (backend 503)") is left asserting `could_not_identify`, unchanged - checked by hand against `build.js`'s source before deciding not to touch it.

## Proof (pasted, not summarised)

**`agents/test/orchestrator.test.js`** (`cd agents && node --test test/orchestrator.test.js`):
```
ℹ tests 25
ℹ suites 0
ℹ pass 25
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

**`cd agents && JURAH_API_BASE=https://tryjuraaah.vercel.app/api/agent npm run verify`** (unit tests, rebuild, generated-workflow checks):
```
ℹ tests 143
ℹ pass 143
ℹ fail 0
...
all checks passed
```
This includes the re-pointed TC-EX-04/TC-AD-14 scenarios, the new photo/medicine-box/other/unsure/PDF/caregiver scenarios, and the static graph-walk check, all `OK`.

**`git diff origin/main...HEAD --stat -- agents/workflows/agent-telegram-inbound.json`** (the only file the rebuild changed under `agents/workflows/`; the other four differ only by this Windows checkout's CRLF and were `git restore`d, confirmed empty with `git diff --ignore-cr-at-eol` - re-verified after the origin/main merge, below):
```
 agents/workflows/agent-telegram-inbound.json | 718 +++++++++++++++++++++++-
 1 file changed, 717 insertions(+), 1 deletion(-)
```
`git diff origin/main...HEAD -- agents/workflows | grep -c 'not[-]deployed'` -> `0`.

**`cd agents/knowledge && JURAH_API_BASE=https://tryjuraaah.vercel.app/api/agent npm run verify`**:
```
ℹ tests 92
ℹ pass 92
ℹ fail 0
...
all checks passed
```

**`git diff --stat -- agents/knowledge/workflows/agent-travel-check.json`** (the only file changed under `agents/knowledge/workflows/`; the other two are Windows CRLF noise, restored):
```
 agents/knowledge/workflows/agent-travel-check.json | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```

**`npx vitest run tests/unit/agent/knowledge-contract.test.ts`**:
```
 Test Files  1 passed (1)
      Tests  28 passed (28)
```

**Owed-marker count** (`grep -rhoE 'TO BE SUPPLIED|TO_BE_SUPPLIED' agents lib | wc -l`) → `21`, unchanged by this branch (nothing here touches a seed value, a citation or the routing dataset itself, which stays `[TO BE SUPPLIED]`).

**Root guards and notes** (`npm run guards`, `npm run notes:check`) → both green, unaffected by this branch's files.

**Em dashes on this branch's own added lines** (the gate step's own check, U+2014, counted on `git diff origin/main -U0`'s `+` lines) -> `0`.

## Owed, and by whom

| What | Who | Why it is not done here |
|---|---|---|
| The routing dataset (at least 15 items, ≥3 box-or-prescription confusions, ≥1 caregiver message) | Mohammad | forbidden to fabricate (`docs/AGENTS-POLISH-PLAN.md` §9); `agents/eval/datasets/routing.json` stays `[TO BE SUPPLIED]` |
| Routing accuracy, ≥95% | AP-15 | needs the dataset above; the adapter and its scoring rule are built and unit-proven here with an injected transport |
| Photos of real foreign medicine packaging, ≥5, brand and ingredient printed | anyone on the team | same rule; travel identification accuracy (≥80%) is AP-15's, unaffected by this branch |
| The tracking-off routing case (a patient with tracking off gets a short reply) | AP-05, or a follow-up CR | the relay payload carries no tracking state (out of this package's scope, per the plan's own cut list); `routing()` routes such an item honestly and it scores as failing until a tracking-off reply exists |
| Import, publish and bind `agent-telegram-inbound` (and `agent-travel-check` if not already live); record journey J10 | the lead, after merge | live n8n step, never done from this worktree |

## Live steps for the lead after merge

1. Export the current `agent-telegram-inbound` workflow (before any change) into this file's history, for the drift record.
2. Rebuild with the real `JURAH_API_BASE`, import, and bind: `Gemini: what is this photo?` → the *existing* Google Gemini (PaLM) API credential (no new credential); `n8n: travel check` → the *existing* Jur'ah inbound secret credential. `agent-travel-check` (`agents/knowledge`) must already be imported and activated on `jurah/travel-check` - this package does not import it.
3. Activate, read the webhook URL back from the activated workflow (`/webhook/`, never `/webhook-test/`), run the drift check.
4. Before sharing any later export of this workflow, strip its `staticData` (it holds pending-choice Telegram file ids, keyed by patient id and message id) - `agents/README.md` says so now, next to the existing Alexa masking note.
5. Record journey J10 (a medicine box on Telegram reaches Travel Check; an ambiguous photo gets the two-button question) once the routing dataset and AP-13's go-live are both done.

## Merge with `origin/main` (AP-10 landed), and the gate run

This branch was resumed and merged forward: `git merge origin/main` (AP-10's screen-on-every-path work, `aaf7cd6`) into `polish/ap-11-orchestrator`, no conflicts (`docs/DECISIONS.md` auto-merged; AP-10 touches `lib/agent-webhooks/index.ts`/`state.ts` and its own test files, none of which AP-11 touches). After the merge, re-run, fresh, with `JURAH_API_BASE=https://tryjuraaah.vercel.app/api/agent`:

- `cd agents && npm run verify` -> `ℹ tests 143 / pass 143 / fail 0` / `all checks passed`. `git diff --stat -- agents/workflows` after `git restore` on the four CRLF-only files -> empty (only `agent-telegram-inbound.json` is this branch's own change, already committed).
- `cd agents/knowledge && npm run verify` -> `ℹ tests 92 / pass 92 / fail 0` / `all checks passed`. Same CRLF-only restore on the other two workflow files; `agent-travel-check.json`, `agent-extraction.json`'s sibling `agent-interaction-screening-ddinter.json` diffs were pure CRLF (`--ignore-cr-at-eol` empty), restored, not committed.
- `cd agents && node --test test/eval-harness.test.js test/eval-contracts.test.js` -> `ℹ tests 28 / pass 28 / fail 0`.
- `cd agents && node eval/run.js` -> exit code 1, `0 of 6 sets measured; 0 passed`, includes verbatim `NOT MEASURED: routing has 0 of 15 items` (the routing dataset stays owed; this is the harness working as designed, not a failure of this branch).
- `git diff origin/main -U0 -- . ':!docs/backend-notes/ap-11.md' | grep '^+' | grep -c` the U+2014 character -> `0`. `grep -rhoE 'TO BE SUPPLIED|TO_BE_SUPPLIED' agents lib | wc -l` -> `21`, unchanged. `git diff origin/main...HEAD | grep -c not-deployed` -> `0`.

**Root gate (`npm run verify`), run against this merged branch.** `styles/tokens.css` needed one `npm run build:tokens` re-run before `check:tokens` passed (this Windows checkout's CRLF vs. the generator's LF output - confirmed a real no-op with `git diff --ignore-cr-at-eol`, restored and not committed each time, per this repository's own documented pattern for this file). `typecheck`, `lint`, `check:tokens`, `guards` (`All guards passed.`), `seed:diff` (`seed diff — every record present at the stated values, no extra record`) and `notes:check` (all seven sections plus the two shape tables green) all passed standalone. `next build` (via `npx next build`, since the chained `npm run verify` never reached it - see below):
```
✓ Compiled successfully in 57s
  Running TypeScript ...
  Finished TypeScript in 2.7min ...
  Collecting page data using 11 workers ...
  Generating static pages using 11 workers (74/74) ...

ƒ Proxy (Middleware)

○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand

EXIT_CODE:0
```

The one phase that did not go clean in one pass was `npm run test` (the full `vitest run`, ~115 test files, run three times total across this session):
1. First attempt failed only on `check:tokens` (the CRLF false-positive above; fixed as described).
2. Second attempt (tokens fixed): `1 failed | 114 passed (115)` test files, `1086 passed | 1 skipped` tests, `11` unhandled `[vitest-pool-runner]: Timeout waiting for worker to respond` errors (`.tsx` component-test workers that never started within the pool's own timeout). The one real assertion failure: `tests/unit/agent/routes.test.ts` > "the gate - every route x every user role" > the `patient (حمد)` case, `Error: Test timed out in 5000ms`.
3. Third attempt (nothing else running in this worktree meanwhile): `3 failed | 1029 passed | 1 skipped (1033)` tests, `19` of the same pool-timeout errors. All three real failures were the identical error shape, `Error: Test timed out in 5000ms`: two more cases of the same `routes.test.ts` describe block, and one in `tests/unit/assistant/assistant.test.ts` ("askAssistant ... the id is the SESSION's, the secret header is sent").

None of the three files behind any of these (`routes.test.ts`, `assistant.test.ts`, or any of the 11+19 pool-timeout `.tsx` files) is in AP-11's own file list, is touched by this branch's diff, or was touched by the AP-10 merge (`git diff c53ca22 origin/main -- <file>` is empty for `routes.test.ts` and the whole `tests/unit/assistant/` and `lib/assistant/` trees). Re-run in isolation (a single `npx vitest run` invocation, nothing else competing for the machine): every one of `tests/unit/agent/routes.test.ts`, `tests/unit/assistant/assistant.test.ts`, `tests/unit/identity/sign-in-form.test.tsx`, `tests/unit/ambient/SettingsScreen.test.tsx` and `tests/unit/prescription/AddPrescriptionFlow.test.tsx` passes cleanly (`Test Files 4 passed (4)` / `Tests 61 passed (61)` the first time; `Test Files 2 passed (2)` / `Tests 54 passed (54)` the second), each run in single-digit seconds. `vitest`'s own summary names the cause: "Environment jsdom was created 107-115 times ... 63-64% of tracked time" - this machine cannot start ~115 parallel forked jsdom workers within their own timeouts, a resource ceiling of this Windows worktree, not a regression. Per this run's own instruction ("when a suite fails broadly, debug two or three first, ... rerun only the failed tests on a fresh server"): done, above: the failures do not reproduce outside the full-parallel run, so they are reported as environment flakiness, not attributed to this package. A fourth full-suite attempt was not run: two consecutive attempts showed the identical failure shape (5000ms timeouts) growing with machine load, not shrinking, so a third attempt was judged unlikely to add information a reviewer could not already get from the isolation runs above.

## Two more merges (`main` kept moving while this PR was open), and one real fix among them

`main` is a fast-moving trunk in this run: PR #16 (AP-18, robustness) and then PR #13 (AP-11a, the app half of D6/CR-078) both merged to `main` after this branch was first pushed and its PR opened. Rather than leave the PR showing GitHub's `CONFLICTING` mergeable state, both were merged into this branch in turn, in the order they landed:

1. **AP-18** (`5ba0a3a`). No overlap in substance: AP-18 touches `agents/package.json` and `agents/scripts/drift.js` among the files this branch also has, and neither collides with this branch's own edits to `agents/scripts/build.js`/`check.js`. The only real conflict was `docs/DECISIONS.md` (both branches append at the same anchor point) - resolved by keeping both appended sections, one after the other, no content from either side changed. One thing this merge surfaced, diagnosed and left exactly as found (not this package's file, not touched): `agents/test/error-workflow.test.js`'s `checkCommittedFile` failed once, on the very first `cd agents && npm run verify` run after the merge, with `agents/workflows/agent-error.json is not what scripts/build-error-workflow.js currently generates`. Same class of issue as `styles/tokens.css` (this Windows checkout hands a freshly-merged file CRLF; a fresh in-memory build is LF) - confirmed with `git diff --ignore-cr-at-eol` (empty) after running `node scripts/build-error-workflow.js` once, then the file was `git restore`d, never committed. Unlike `styles/tokens.css`, `agent-error.json`'s own rebuild happens to run *after* the test that reads the stale checked-out copy in `agents`' own `verify` chain (`test && build && check`), so a bare fresh checkout hits this once; `agents/scripts/build-error-workflow.js` is AP-18's own file, outside this package's reserved list, so nothing was changed there. Re-run clean: `ℹ tests 157 / pass 157 / fail 0` / `all checks passed`.
2. **AP-11a** (`e9da34b`, PR #13, the app half of D6/CR-078 that CR-095 already named as "the closer"). Textually clean (no conflict markers) but semantically wrong in one file, caught only by actually running it rather than trusting a clean merge: `tests/unit/agent-webhooks/core.test.ts`. PR #13 brings in the real fix (`lib/agent-webhooks/core.ts` `readDrugCheck` now passes `cannot_verify` through unchanged) and, in its own branch, had reverted the "every appOutcome ... unchanged" test back to its original unconditional form (no longer needing an exception) plus added a new dedicated `cannot_verify (CR-078) passes through ...` test. Because PR #13's diff for that *specific, pre-existing* test hunk was empty relative to the true common ancestor (it only added new content after it), git's 3-way merge saw one side (this branch) editing that hunk and the other side leaving it alone, and kept this branch's now-obsolete stopgap (`const expected = appOutcome.kind === 'cannot_verify' ? { kind: 'could_not_identify' } : appOutcome`) untouched - which now asserts the *old*, pre-PR-#13 fallback against the *new*, real `readDrugCheck`. Run immediately after the merge: `AssertionError: expected { kind: 'cannot_verify' } to deeply equal { kind: 'could_not_identify' }`. Fixed by restoring the plain, unconditional assertion (matching PR #13's own version); `docs/DECISIONS.md`'s CR-095 entry updated to record this rather than leave the earlier "comment naming PR #13 as the closer" text stale. `tests/unit/agent/knowledge-contract.test.ts` (the OTHER file both branches touch) needed no change: PR #13 there just reverts to what its own ancestor had, and this branch's `cannot_verify` allowance in that file's shape check is a genuinely separate, still-needed change (the agent's own `travel-check.js` producing `cannot_verify` at all is this branch's own CR-095, not PR #13's).

**Re-verified after both merges, fresh:** `cd agents && JURAH_API_BASE=... npm run verify` -> `ℹ tests 157 / pass 157 / fail 0` / `all checks passed`. `cd agents/knowledge && JURAH_API_BASE=... npm run verify` -> `ℹ tests 92 / pass 92 / fail 0` / `all checks passed`. `npx vitest run tests/unit/agent-webhooks tests/unit/agent/knowledge-contract.test.ts tests/unit/supply` -> `Test Files 12 passed (12)` / `Tests 107 passed (107)`. Every workflow JSON diff `git status` showed afterward was confirmed CRLF-only (`--ignore-cr-at-eol` empty) and restored, never committed. `grep -rhoE 'TO BE SUPPLIED|TO_BE_SUPPLIED' agents lib | wc -l` -> `23` now (was `21`; the `+2` is AP-18's own `TEAM_CHAT_ID` placeholder in `agents/lib/error-alert.js`'s generated node and its own committed `agent-error.json`, not this package's).

The full root `npm run verify` (the three-attempt run documented above) was **not** re-run a fourth time after these two merges: neither merge touched any file under `app/`, `components/`, `lib/` (other than the one already-covered `lib/agent-webhooks/core.ts`), `features/`, or any root vitest suite besides the ones just re-verified targeted above, so a full 12-minute re-run of ~115 files already shown to be flaky under this machine's own parallel-worker ceiling was judged not worth repeating for files this package does not touch. If the trunk moves again before this merges, whoever does the actual merge-to-`main` step should expect the same pattern: a clean auto-merge is not proof of a correct one, particularly for a file two packages both edited for the same underlying reason from two different angles.

## Acceptance, against the plan's own line

Tests per route: `agents/test/orchestrator.test.js` (25, one or more per route named in the plan) and the re-pointed/new `agents/scripts/check.js` scenarios, both green. `cd agents && npm run verify` and `cd agents/knowledge && npm run verify` (with `JURAH_API_BASE`) both green. Root `npm run verify`: `typecheck`/`lint`/`check:tokens`/`guards`/`seed:diff`/`notes:check`/`next build` all green standalone; the chained run's `test` step surfaces machine-level worker-timeout flakiness in files this package does not own or touch (isolated re-runs green; detailed above), so the chained command itself did not reach `seed:diff`/`notes:check`/`next build` - each was proven green in its own separate command instead, pasted above. Routing accuracy stays AP-15's, owed the dataset above.

## Gate B rerun (2026-09-25), fresh on the same merged branch, one new finding

`git fetch origin && git merge origin/main` -> `Already up to date` (`git log --oneline HEAD..origin/main` is empty; nothing landed on `main` since the merges above). Nothing to merge, nothing to re-resolve. Every command below was run standalone rather than as one chained `npm run verify`, for the same reason as this file's own precedent above: this Windows checkout hands `styles/tokens.css`, and this run also `agents/workflows/agent-error.json`, back CRLF the moment either is freshly checked out or `git restore`d, while their generators write LF - `git diff --ignore-cr-at-eol` confirmed empty (a real no-op) each time before the file was restored and left uncommitted.

**Root `npm run verify`, green.** `typecheck`, `lint` both pass silently. `check:tokens` needed one `npm run build:tokens` first (confirmed CRLF-only, restored). `guards` -> `All guards passed.` `npm run test` (the full `vitest run`, 128 files): first pass `Test Files 1 failed | 127 passed (128)` / `Tests 1 failed | 1224 passed | 1 skipped (1226)`, the one failure `tests/unit/agent/routes.test.ts` > "the gate - every route x every user role" > "patient (حمد): session cookie, no bearer -> 403 ...", `Error: Test timed out in 5000ms` - the same file and the same worker-pool-timeout shape already named above as environment flakiness, not this package's. Rerun alone: `Test Files 1 passed (1)` / `Tests 37 passed (37)`, 4.9s. Per the standing rule ("a failing test file: rerun it alone; passes alone -> rerun the full gate once more"): full suite rerun, `Test Files 128 passed (128)` / `Tests 1225 passed | 1 skipped (1226)`, exit 0. `seed:diff` -> `seed diff — every record present at the stated values, no extra record`. `notes:check` -> all seven sections plus both shape tables green. `next build` (`npx next build`) -> `Compiled successfully in 13.8s`, `Finished TypeScript in 43s`, 74/74 pages, exit 0.

**`cd agents/knowledge && JURAH_API_BASE=https://tryjuraaah.vercel.app/api/agent npm run verify`, green.** `tests 92 / pass 92 / fail 0`, `all checks passed`. The two non-`agent-travel-check` workflow files the rebuild touched (`agent-extraction.json`, `agent-interaction-screening-ddinter.json`) were CRLF-only (`--ignore-cr-at-eol` empty), restored, not committed. `agent-travel-check.json` (this package's own file) already matched the rebuild exactly, no diff at all.

**`cd agents && JURAH_API_BASE=https://tryjuraaah.vercel.app/api/agent npm run verify`, RED - one new finding, not this package's.** `test`: `tests 157 / pass 157 / fail 0`. `build`: regenerates all five other workflow files; every one of `agent-alexa.json`, `agent-checkin-daily.json`, `agent-error.json`, `agent-interaction-screening.json`, `agent-webchat.json` came back CRLF-only (`--ignore-cr-at-eol` empty each), restored, not committed; `agent-telegram-inbound.json` (this package's own rebuilt file) already matched. `check` (`node scripts/check.js && node scripts/check-error-workflow.js`) -> **4 check(s) FAILED**, all the same shape:

```
FAIL  «شنو جرعتي الجاية» -> the next OPEN dose, spoken in Arabic, session stays open      (agent-alexa)
FAIL  «نسيت دواي» -> names the passed dose, records nothing, sends ITS buttons ...        (agent-alexa)
FAIL  "what is my next dose" in en-US -> English                                          (agent-alexa)
FAIL  «شنو جرعتي الجاية» -> the next OPEN dose from the backend, in Arabic                 (agent-webchat)
```

Every one expected the fixture day's LAST dose (23:55, Calcium carbonate + vitamin D3) and got the FOLLOWING day's FIRST dose instead (00:05, Eltroxin) - the exact shape of the real clock crossing midnight mid-run. This verification session started 2026-09-24 and this command ran after 2026-09-25 00:00 Kuwait time (the machine's own configured zone is `Asia/Kuwait`, confirmed with `node -e "console.log(Intl.DateTimeFormat().resolvedOptions().timeZone)"`, not just a similar-looking local clock). The fixture these four scenarios share (`DAY`, `agents/scripts/check.js` line 106, `date: '2026-09-24'`) is one absolute calendar day; the "next open dose" all four compute is read against the real `Date.now()`/`new Date()` (`check.js` lines ~478, ~483, ~755), never a frozen reference - unlike `agents/lib/orchestrator.js`'s own scenarios, which thread `{store, now}` through `routeInbound` for exactly this reason, this pre-existing code path has no injectable clock at all. The moment real time passed 23:55 on the 24th, every dose in the fixture read as already past, and "next dose" fell through to the following, unfixtured day.

**Confirmed not this package's regression, the same way `agent-error.json` was confirmed above.** All four scenario functions and the `DAY` fixture they share are byte-identical in `origin/main`'s own `agents/scripts/check.js` (`git show origin/main:agents/scripts/check.js`, same four descriptions at their own line numbers; none inside any hunk `git diff origin/main...HEAD -- agents/scripts/check.js` touches - confirmed by inspecting every hunk this branch's diff to that file contains). Run against `origin/main` itself, at this same real moment, `cd agents && npm run verify` would fail identically: this is a pre-existing gap in the shared harness (no frozen "now" for these four scenarios), not something this package introduced. A real fix means giving these four scenarios their own injectable clock, the same discipline `CLAUDE.md` rule 9 already asks of the app itself - that touches scenario code this package did not write and has not fully audited, so it is logged here rather than patched quietly, by the same standard this file already applied to `agent-error.json`/AP-18 above.

**Not the run's named exception.** This is a different, new failure from the one already excused for this run (`tests/e2e/audit-2026-09.spec.ts` "audit C6", e2e, already red on `main` at `e9be92d`) - that one was not touched or re-run here (no screen touched, e2e out of scope for this package). This one is reported, not excused: `root npm run verify` and `agents/knowledge npm run verify` are green on this run; `agents npm run verify` is red, only on these four pre-existing, calendar-date-anchored scenarios (`test` 157/157 and `build` both still pass). Whoever re-runs this gate before the next Kuwait midnight, or after `check.js` gets a frozen clock for these four scenarios, should see all three commands green.
