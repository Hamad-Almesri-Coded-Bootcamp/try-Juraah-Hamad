# AP-19 · Documents (with AP-15, accuracy runs)

Branch `polish/ap-19-documents`, cut from `origin/main` at `feeee90` (2026-09-25). At that moment
PR #22 (AP-04, "one screening") was `OPEN`; PR #15 (AP-07, SFDA brands) had just `MERGED`
(`feeee907b04aefaef7e9efa2f6279b5394518cb3`, 2026-09-25T10:00:46Z) — after this branch's plan was
written but before this branch was cut, so its content is already in this base, not a still-open PR.
PR #22 itself merged to `main` (`9a6f449`) while this branch's six files were being written; this
branch then merged `origin/main` (`9a6f449`) in turn, resolved the two expected README conflicts,
and every count and status line below is re-measured on that merged tree, not on `feeee90`.

## What was stale

| File | What was stale | Evidence |
|---|---|---|
| `agents/README.md` | "Three n8n workflows" (5, after AP-04/PR #22 retired the legacy screening; 6 before it); "36 tests" (167 measured, post-merge); "the `ai-agents` branch (or `Backend` once merged)" (merged into `main` since PR #2, 2026-09-23); "Activate all three" (5-6 files, never 3); no voice-turns/screen-follows-voice/"What to say" section; no `npm run drift` or `node eval/run.js` command | `ls agents/workflows/*.json` = 5 (post-merge); `cd agents && npm test` = 167 (post-merge); `git log` PR #2 |
| `agents/knowledge/README.md` | "89 unit tests" (111 measured, post-merge, after AP-07/PR #15 and AP-04/PR #22 both added tests); Going live step 1 said the CR-062 route was "on the `ai-agents` branch and not yet on `main`" (it has been on `main` since PR #2); the TC-IX-02 bullet paraphrased `ALERT_TEXT.nothingFound` instead of quoting it; the screening-URL bullet still described the app calling a fourth webhook itself instead of the backend's own `screenOrHold`; "Not run end to end" cited an unset agent token, which cannot be checked from here; the extraction-overlap bullet asked the team to decide, when D3/CR-075 already decided; the accuracy bullet said neither target "is claimed" with no pointer to the harness | `cd agents/knowledge && npm test` = 111 (post-merge); `docs/DECISIONS.md` CR-074, CR-075, CR-090, CR-100 |
| `docs/API-SURFACE.md` | Section B had no row for `POST /api/agent/patients/{patientId}/voice-turns` (migration 0012, `lib/agent/handlers.ts` `postVoiceTurn`); the `/api/messaging/telegram/open` row's Auth column did not name the opaque `null`-Origin refusal or the `Sec-Fetch-Site` check, though the route (`app/api/messaging/telegram/open/route.ts` `isSameOriginPost`) already makes both; the `/api/agent/alerts` row did not say the route answers `201 { alert, delivered }` or that the patient's own Telegram send depends on `Settings.notificationChannel` (CR-105) | `for r in $(git ls-files app/api ...)` found exactly one MISSING route on this base: voice-turns |
| `docs/SCHEMA.md` | Section 2 said "Server-side tables (6)" and had no `voice_turns` row; section 3's RLS table had no `voice_turns` row | `supabase/migrations/0012_voice_turns.sql`, `0013_voice_turns_record_topic.sql` |
| `docs/VERIFICATION.md` | Section 12 still carried "The six AI agents are not built | nothing under `lib/agent/**` calls a model ..." (line 355), stale since the `ai-agents` merge (PR #2, 2026-09-23); no section reported AP-15's data-based accuracy runs or AP-14's live journeys at all | line 355, this branch's own `sed -n` reading before the edit |

## What changed

```
$ git diff --stat origin/main...HEAD
 agents/README.md            |  96 ++++++++++++++++++----
 agents/knowledge/README.md  |  14 ++--
 docs/API-SURFACE.md         |   5 +-
 docs/SCHEMA.md              |   4 +-
 docs/VERIFICATION.md        |  78 +++++++++++++++++-
 docs/backend-notes/ap-19.md | 193 ++++++++++++++++++++++++++++++++++++++++++++
 6 files changed, 362 insertions(+), 28 deletions(-)
```

Six files, all documents; `git diff --name-only origin/main...HEAD` names exactly these six and
nothing in `app/`, `i18n/copy/`, a route or a test file — no e2e run is owed for this PR.

## Proof

**`cd agents && npm test`** (167 tests, post AP-04/PR #22 merge):
```
ℹ tests 167
ℹ pass 167
ℹ fail 0
```

**`cd agents/knowledge && npm test`** (111 tests, post AP-04/PR #22 and AP-07/PR #15):
```
ℹ tests 111
ℹ pass 111
ℹ fail 0
```

**`npx vitest run tests/unit/agent/agents-contract.test.ts tests/unit/agent/knowledge-contract.test.ts`**:
```
 Test Files  2 passed (2)
      Tests  37 passed (37)
```

**`grep -rhoE 'TO BE SUPPLIED|TO_BE_SUPPLIED' agents lib | wc -l`**: `18` (was `23` before AP-04/PR #22
retired the legacy screening's five markers).

**`cd agents && node eval/run.js; echo exit=$?`** (unchanged by this branch; pasted into
`docs/VERIFICATION.md`'s new "Agents, accuracy" section):
```
NOT MEASURED: extraction has 0 of 10 items
NOT MEASURED: adherence has 0 of 20 items
NOT MEASURED: screening-interacting has 0 of 3 items
NOT MEASURED: screening-non-interacting has 0 of 3 items
NOT MEASURED: travel has 0 of 5 items
NOT MEASURED: routing has 0 of 15 items

0 of 6 sets measured; 0 passed - exit code 1: every set must be measured and pass
exit=1
```

**`npm run notes:check`** (root, standalone):
```
✓ ## 1. The data-access surface
✓ ## 2. Where the mock cheated
✓ ## 3. Rules that are currently absences, and must become refusals
✓ ## 4. Fields a screen depends on
✓ ## 5. The shapes, verbatim
✓ ## 6. Deferred to Phase 2 by design
✓ ## 7. Things Phase 1 found out the hard way
§1 rows for lib/data/api.ts: 50/50
§5 shapes for lib/data/api.ts: 50/50
§1 rows for lib/session/api.ts: 5/5
§5 shapes for lib/session/api.ts: 5/5
notes:check exit=0
```

**Root `npm run verify`** (typecheck, lint, `check:tokens`, guards, test, `seed:diff`, `notes:check`,
`next build`; `.next` moved out of the worktree first). First pass: `check:tokens` failed with an
empty `git diff --ignore-cr-at-eol styles/tokens.css` (the Windows-checkout CRLF-vs-generator-LF
noise this repo already documents for this file); `npm run build:tokens` then re-run, `verify`
re-run clean, `styles/tokens.css` restored with `git restore` and never committed:
```
✓ styles/tokens.css is in sync with docs/design-system/tokens.json
All guards passed.
 (full unit suite passed; seed:diff PASS 72/72; notes:check as above)
▲ Next.js 16.3.5 (Turbopack)
✓ Compiled successfully in 30.3s
  Generating static pages ...
VERIFY_EXIT=0
```

## PR #22, and how its lines were merged

PR #22 (AP-04, "one screening") was `OPEN` at the start of this branch. Its own diff (`gh pr diff
22`) was read before editing, so the lines it was expected to rewrite were left exactly as they
were on this branch's base, for PR #22 to change when it merged:

- `agents/README.md`: the workflow diagram's `prescription →` branch (the line ending
  "→ n8n agent-interaction-screening, when saved and unflagged"), the whole
  `n8n agent-interaction-screening (x-jurah-secret) { patientId, newPrescriptionId }` block, the
  "Drug-knowledge agents" paragraph, and Going live step 4.
- `agents/knowledge/README.md`: the Interaction Screening and Extraction arrows in the top code
  block, Going live steps 3, 4 and 7, and the "A prescription confirmed by the reviewer is not
  re-screened automatically ... (CR-065)" bullet under Honest limits.

PR #22 merged (`9a6f449`) before this branch did. `git merge origin/main` produced exactly the two
expected conflicts, both resolved by taking PR #22's rewritten text for the lines above and this
branch's own text for its own lines (`agents/README.md`'s Going live step 5 fix and its "Not yet
proven" rewrite minus the "screening reference set" sentence PR #22 deleted; `agents/knowledge/
README.md` keeps every "Honest limits" bullet from both sides). After the merge: the workflow count
(6 → 5 in `agents/workflows/`, 8 total), the test counts, the root contract count and the CR-074,
CR-075, CR-090, CR-098 and new CR-100 rows below were all re-measured and updated on the merged
tree, not left as they stood on `feeee90`.

CR-100 arrived with PR #22 (`9a6f449`): its own diff appended the `docs/DECISIONS.md` entry
(`git log -S'### CR-100' origin/main -- docs/DECISIONS.md` → `0a2412d`, "5/6: AP-04 documents and
the CR (D2/CR-074)"). Every agents CR number the plan names from CR-062 on — including CR-086,
CR-087, CR-094 and CR-095, which read as missing in an earlier draft of this note until a second
pass re-read `docs/DECISIONS.md` fresh rather than trusting an earlier read taken before this branch
was cut from `origin/main` — is present and is a row below.

## Proposed status lines, for the owner

`docs/DECISIONS.md` is not edited on this branch. Every line below is proposed for the owner —
Mohammad, on Hamad's delegation — to accept, correct or decide otherwise; nothing here says
"accepted by the owner" on its own authority.

| CR · heading | Status line today (verbatim) | Proposed status line | Evidence on main | Still owed, by whom |
|---|---|---|---|---|
| CR-062 · Two agent READ routes the agents track cannot work without | `PROPOSED` (raised by the AI-agents track, branch `ai-agents`, 2026-09-23) | BUILT (PR #2). | PR #2 (`cd94aaf`); both routes live under `app/api/agent/patients/{patientId}/...` | none |
| CR-063 · The Telegram webhook forwards chat replies to the agents track | `PROPOSED` (raised by the AI-agents track, branch `ai-agents`, 2026-09-23) | BUILT (PR #2). | PR #2; `lib/agent/inbound.ts#relayReply` | none |
| CR-064 · The agents run on the real clock; the app runs on the frozen one | `DECIDED: (b), real time in production` | DECIDED and BUILT: option (b). | commit `b7228d2` on `main`; `lib/config.ts` `kuwaitNow()` branches on `VERCEL_ENV` (`git log -S'VERCEL_ENV' -- lib/config.ts`) | none |
| CR-065 · The drug-knowledge agents (Interaction Screening on DDInter, Travel Check, Extraction) | `PROPOSED` (raised by the AI-agents track, Waddah, branch `ai-agents-knowledge`, 2026-09-23) | BUILT (PR #2); the three workflows and `agents/knowledge/**` are on `main`. | PR #2 | Warfarin × Ibuprofen citation (CR-096); MAREVAN/LIPITOR SFDA verification (CR-065's own text, partly closed by AP-07/PR #15 for BRUFEN/GLUCOPHAGE) |
| CR-066 · Let the app's drug check and prescription intake call the agents | `BUILT` 2026-09-23 | BUILT (PR #2), unchanged. | PR #2; `lib/agent-webhooks/**` | the three `JURAH_AGENT_*_URL` values, a live/Vercel step (AP-13), not a code gap |
| CR-067 · A read-only assistant in the patient shell | `PROPOSED` (raised by the AI-agents track, branch `ai-agents`, 2026-09-23) | BUILT (PR #2). | PR #2; `features/assistant/AssistantLauncher.tsx`, `lib/assistant/**` | none |
| CR-068 · Recording a dose from the web assistant | `PROPOSED, NOT BUILT` (raised by the AI-agents track, 2026-09-23) | CLOSED, NOT BUILT (CR-076). | CR-076's own heading, `DECIDED: no` | none |
| CR-069 · The screen follows the voice (Alexa → the patient's open web app) — the agents entry, not the UX audit heading of the same number | `BUILT` (raised by the AI-agents track, 2026-09-23) | BUILT; amended by CR-102 (PR #14): the voice turn now moves the page only, and the assistant panel no longer opens by itself. | commit `5de3c2c` (pushed to `main` directly); PR #14 (`c53ca22`) | none |
| CR-070 · Free talk with Alexa, and recording doses by voice — the agents entry, not the UX audit heading of the same number | `BUILT`, switch **OFF in the repository**, **ON in the live demo node** (decided by the AI-agents track, Mohammad, 2026-09-24, without the owner's review) | Free talk BUILT; recording by voice reversed by CR-073 and removed by CR-101 (PR #8); the live node changes only when the lead republishes `agent-alexa`, and J12 proves it. | PR #8 (`1a774a7`); `docs/backend-notes/ap-01.md`'s drift report (`VOICE_RECORDS` live vs. repository) | the live republish itself (AP-13, the lead) |
| CR-073 · D1 · Voice records no dose status | `DECIDED: off, write path removed` | DECIDED and BUILT (PR #8, AP-02; CR-101 records the built shape). | PR #4 (`04d086c`, the decision); PR #8 (the build) | J12's runtime proof and the live republish (both AP-14/AP-13, the lead) |
| CR-074 · D2 · One Interaction Screening: the DDInter workflow | `DECIDED` | DECIDED; BUILT (PR #22, executed as CR-100). | PR #4 (the decision); PR #22 (`9a6f449`) | the live cutover itself (`docs/backend-notes/ap-04.md`'s exact-order steps, the lead) |
| CR-075 · D3 · One Extraction core: `agents/knowledge/src/extraction.js` | `DECIDED` | DECIDED; BUILT (CR-098/PR #12, its item 1 closed by CR-100/PR #22). | PR #4 (the decision); PR #12 (`fd0e516`); PR #22 (closes the last item) | none |
| CR-076 · D4 · CR-068: the web assistant records nothing | `DECIDED: no` | DECIDED: no; closed, nothing further to build. | PR #4 | none |
| CR-077 · D5 · TC-IX-02: keep the one `info` `auto_cleared` "screened" marker | `DECIDED: keep, logged as a deliberate reading` | DECIDED: keep, as built (`agents/knowledge/src/screening.js`, `ALERT_TEXT.nothingFound` in `src/text.js`). | PR #4 (the decision); PR #2 (the original build) | none |
| CR-078 · D6 · CR-066: add `cannot_verify` to `DrugCheckOutcome` | `DECIDED` | DECIDED; BUILT (PR #13, the app side; PR #20, the agent side). | PR #4 (the decision); PR #13 (`e9da34b`, AP-11a); PR #20 (`70d53c3`, AP-11b) | none |
| CR-079 · D7 · Register of agent replies | `DECIDED: Fusha in text the app shows; dialect stays in Telegram and on Alexa` | DECIDED; PARTIALLY BUILT (PR #18, AP-17: Fusha web chat replies, two Fusha Alexa samples). | PR #4 (the decision); PR #18 (`8247121`, commit `636d09e`) | CR-103 builds further on this decision (below): the owner's or the lead's choice between its option (a), translate at display, and option (b), store both languages |
| CR-080 · D8 · Where live tests write | `DECIDED: a Supabase branch for integration runs; production with the two D-041 demo patients for the journeys, reset afterwards` | DECIDED; not yet exercised. | PR #4 | the Supabase branch (cost-approved) and the D-041 patients' journeys, both AP-14's (the lead) |
| CR-081 · D9 · D-035: an agent may overwrite a recorded status | `DECIDED: keep overwrite` | DECIDED: keep overwrite; a policy decision only, nothing to build. | PR #4; `doses_status_write` already permits an agent overwrite by construction | none |
| CR-082 · D10 · "A refill passes through screening" | `DECIDED: re-screen the prescription's pairs when a refill is requested` | DECIDED; BUILT (PR #10, AP-10; CR-090). | PR #4 (the decision); PR #10 (`aaf7cd6`) | the duplicate-alert-on-refill follow-up (CR-090 iii / CR-100, PR #22) |
| CR-083 · D11 · The Telegram link token lives only in a server redirect | `DECIDED: yes` | DECIDED: yes; BUILT (PR #11, AP-09). | PR #4 (the decision); PR #11 (`f6f484a`); `app/api/messaging/telegram/open/route.ts` | none |
| CR-084 · D12 · A "being checked" state on a new prescription | `DECIDED: build it` | DECIDED: build it; BUILT (PR #10, AP-10; CR-089). | PR #4 (the decision); PR #10 | none (CR-088's retry record would make the state exact, but that is CR-088's own gap) |
| CR-085 · D13 · The real bot handle, and who registers the app as the bot's webhook | `DECIDED: the handle is the one Telegram reports for the configured bot token (getMe); the webhook stays registered to the app` | DECIDED; BUILT (PR #11, AP-09): `botUsername()` calls Telegram's `getMe`. | PR #4 (the decision); PR #11; `app/api/messaging/telegram/open/route.ts` | `JURAH_BOT_TOKEN` itself, a live secret (AP-13, Hamad) |
| CR-086 · AP-09 · Telegram linking from the app: one form, one route, the token only in its redirect | `Accepted by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end` · `BUILT` 2026-09-24 (raised by AP-09, branch `polish/ap-09-telegram-link`) | BUILT (PR #11). | PR #11 (`f6f484a`); `app/api/messaging/telegram/open/route.ts`, `features/ambient/TelegramLinkForm.tsx` | none |
| CR-087 · AP-09 · The waiting state: "I pressed Start", five minutes of checks, a calm "still waiting" line | `Accepted by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end` · `BUILT` 2026-09-24 (raised by AP-09, branch `polish/ap-09-telegram-link`) | BUILT (PR #11): `useLinkConfirmation` in `TelegramLinkForm.tsx`. | PR #11 | none |
| CR-091 · AP-12: the audit insert policy checks the actor a row names (migration 0014) | `PROPOSED` (raised by AP-12, 2026-09-24; needs 0014 applied to production, which merging does not do) | BUILT in the repository (PR #6); PROPOSED for production until 0014 is applied. | PR #6 (`4881ffb`); `supabase/migrations/0014_audit_insert_actor.sql` | applying 0014 to production (Hamad's yes, the lead); the entry's own items 1-4; `docs/SCHEMA.md` §3 and a new `docs/ENFORCEMENT.md` row once applied |
| CR-096 · The real `sourceCitation` for the seed's `ia-001` (Warfarin × Ibuprofen) | `PROPOSED` (raised by AP-06, 2026-09-24) | Stays PROPOSED. | PR #7 (`f7a79b4`, AP-06) computed the citation text; not yet approved | Hamad's approval, then the seed and production edit (the lead) |
| CR-097 · An absent pair is "no interaction" only when one drug's DDInter category file was loaded | `Accepted by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end` (raised by AP-06, 2026-09-24) | ACCEPTED; BUILT (PR #7, AP-06). | PR #7 | loading a further DDInter category file needs the owner's yes (the entry's own text) |
| CR-098 · AP-03 · One extraction core in both paths (D3, CR-075) | `PARTIALLY BUILT, timeboxed` (raised by AP-03, 2026-09-24) | BUILT: items 2-5 closed at the PR #12 merge gate; item 1 closed by CR-100/PR #22 (the hand-off node no longer exists — any unscreened save now fails the execution outright, "a stronger closing than the original item asked for," in CR-100's own words). | PR #12 (`fd0e516`); PR #22 (`9a6f449`) | none |
| CR-099 · AP-08 · How the evaluation harness reads the five data-based pass criteria | `PROPOSED` (raised by the AP-08 package, 2026-09-24, for Hamad; built as proposed, each reading one list or rule to change) | Stays PROPOSED. | PR #9 (`e9be92d`, AP-08); `agents/eval/thresholds.js`, `agents/eval/sets.js` | Hamad's confirmation of the readings; dependency (a), routing via AP-11, is now closed (PR #19 merged) |
| CR-101 · AP-02 · Voice back inside rule 1, as built | `ACCEPTED by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end` · `BUILT` (branch `polish/ap-02-voice-inside-rule-1`) | BUILT (PR #8). | PR #8 (`1a774a7`) | J12's runtime proof (AP-14); the ar-SA Alexa console model rebuild (Mohammad, live step) |
| CR-088 · A retry record for screening requests n8n did not accept | `PROPOSED` (AP-10, 2026-09-24; needs a production migration, so not built) | Stays PROPOSED; not built. | no PR builds this table; PR #10 built `screenOrHold`'s hold-only behaviour, not the retry table | migration 0015, the retry job route, the cron line (the lead, on the owner's yes) |
| CR-089 · A "being checked" state on a new prescription, on B2 and B3 | `BUILT` · Accepted by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end (D12 / CR-084; no schema change, no human-owed value) | BUILT (PR #10). | PR #10 (`aaf7cd6`) | CR-088's retry record would make the state exact (not owed by CR-089 itself) |
| CR-090 · Screening on every path: a reviewer's confirmation, an agent's save and a refill request screen too, and the agent route says so | `BUILT` · Accepted by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end (AP-10, D10 / CR-082; no schema change, no human-owed value) | BUILT (PR #10); items (i) and (ii) closed by CR-100/PR #22, item (iii) still open. | PR #10; PR #22 (`9a6f449`) | (iii), the duplicate-alert-on-refill, is CR-100's own "still open" item |
| CR-100 · AP-04 · One screening: DDInter is the only workflow on `jurah/screen-prescription` | `Accepted by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end` (D2/CR-074 executed; raised by AP-04, 2026-09-25) | ACCEPTED; BUILT (PR #22). | PR #22 (`9a6f449`); `agents/lib/screening.js`, `agents/test/screening.test.js` and `agents/workflows/agent-interaction-screening.json` removed (`git rm`) | the duplicate-alert-on-refill option (an agent read of open alerts, or backend-side de-duplication) — the owner's choice, the backend lane builds it |
| CR-102 · CR-069 amended: a voice turn moves the page only | `DECIDED` · `BUILT` (branch `polish/voice-follow-page-only`) | BUILT (PR #14). | PR #14 (`c53ca22`) | none |
| CR-092 · AP-05 · The CR-063 relay payload carries whether tracking is on | `Accepted by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end` (raised by AP-05, 2026-09-24; built the same day, branch `polish/ap-05-adherence-hardening`) | BUILT (PR #17, AP-05). | PR #17 (`ce7204d`) | the live republish of `agent-telegram-inbound` (AP-13, the lead) before production actually sends `trackingOn` |
| CR-094 · AP-11 · The Orchestrator's own choices | `Accepted by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end` (raised by AP-11, 2026-09-24) | ACCEPTED; BUILT (PR #19). | PR #19 (`eddaace`); `agents/lib/orchestrator.js`, `agents/test/orchestrator.test.js` (25 tests) | the routing dataset itself (still `[TO BE SUPPLIED]`, AP-15); reconciling the tracking-off routing label with `decide()`'s outcome (owed to AP-15, per the entry's own text) |
| CR-095 · AP-11 · The agent-side `cannot_verify` app outcome (the other half of D6/CR-078) | `Accepted by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end` (raised by AP-11, 2026-09-24) | ACCEPTED; BUILT (PR #19, merged with AP-11b/PR #20). | PR #19; PR #20 (`70d53c3`) | none |
| CR-103 · AP-17 · An agent-written alert stays in the patient's language for every OTHER reader | `PROPOSED` (raised by AP-17, 2026-09-24) | Stays PROPOSED. | PR #18 (`8247121`); neither of the entry's two options (translate at display, or store both languages) is built | the owner's or the lead's choice between the entry's option (a) and option (b) |
| CR-104 · The Alexa request signature is not verified | `PROPOSED` (raised by AP-18, 2026-09-24) | Stays PROPOSED. | PR #16 (`5ba0a3a`, AP-18) wrote the research; neither option is built | choosing Option A or B (the owner/Mohammad), then building it |
| CR-105 · `Settings.notificationChannel` and how a patient ever gets `'telegram'` | `PROPOSED` (raised by AP-18, 2026-09-24; the built half is `ACCEPTED by the lead session under the owner's instruction of 2026-09-24 to run the plan end to end`) | The read-side fix is BUILT (PR #16); the write-side proposal stays PROPOSED. | PR #16 (`5ba0a3a`); `lib/agent/notify.ts`, `docs/API-SURFACE.md`'s corrected `/api/agent/alerts` row (this branch) | the owner's choice among the entry's three write-side options, then building it; the lead's pre-merge read of which production patients currently read `'telegram'` |

Not rows in this table: the UX entries `CR-069 · UX audit, 2026-09-23 — what the owner has to
decide` and `CR-070 · UX audit, 2026-09-23 — code fixes` are a different pair of headings sharing
these numbers with the agents CR-069/CR-070 above (`docs/AGENTS-POLISH-PLAN.md` §2 warns to cite
both by heading, never by number alone); `CR-071` (the "Daylight" redesign) and `CR-072` (the day
mark) are UX, not agents, entries. `CR-093` was reserved by AP-05 for a later change (a
discontinuation's callback data carrying the patient's own quote past Telegram's 64-byte limit) and
stays unused: no contract change of that shape has appeared in any package on this base.
