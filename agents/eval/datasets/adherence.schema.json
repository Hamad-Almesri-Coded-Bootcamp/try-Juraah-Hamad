{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Jur'ah evaluation set: adherence replies",
  "description": "Kuwaiti-dialect replies to a dose check-in, each with the intent a native speaker says it carries. Scored as intent accuracy after the workflow's own confidence rule (AI Agents Acceptance Criteria section 2).",
  "type": "object",
  "required": ["set", "items"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "set": { "const": "adherence" },
    "owed": { "type": "string", "description": "The owed-value marker of an empty set. Delete it once the set holds its minimum." },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "text", "expected", "verifiedBy"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]{1,64}$" },
          "text": { "type": "string", "minLength": 1, "maxLength": 500, "description": "The reply exactly as a patient would type it." },
          "expected": { "enum": ["taken_on_time", "taken_late", "missed", "ran_out", "discontinued_by_doctor", "unclear"] },
          "verifiedBy": { "type": "string", "minLength": 2, "description": "The native speaker of Kuwaiti Arabic who confirmed the intent." },
          "suppliedBy": { "type": "string", "minLength": 2 },
          "note": { "type": "string" }
        }
      }
    }
  }
}
