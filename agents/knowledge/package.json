{
  "name": "jurah-agents-knowledge",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Jur'ah - the drug-knowledge agents (Interaction Screening, Travel Check, Extraction) on DDInter + the SFDA brand map, wired to the backend's /api/agent/** routes",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "build": "node scripts/build.js",
    "build:index": "node scripts/build-demo-index.js",
    "build:why": "node scripts/build-why.js",
    "build:why-summaries": "node scripts/build-why-summaries.js",
    "check": "node scripts/check.js",
    "coverage": "node scripts/coverage.js",
    "verify": "npm run test && npm run build && npm run check"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "UNLICENSED"
}
